# 환경변수 (Supabase 키 등 — 절대 커밋 금지)
.env
.env.*

# 원본 데이터 (수백 MB — GitHub 100MB 제한 초과)
data/raw/
data/staging/
*.zip

# 수집·적재 실행 로그 (매 실행마다 새로 생기는 운영 잔재물)
data/logs/

# 감시들이 만드는 이슈 본문 임시 파일 (CI가 gh issue create 에 넘기고 버린다.
# 로컬에서 점검 명령을 돌려도 생기므로 커밋에 딸려 들어가지 않게 막는다)
sangkwon_new_quarter_issue.md
district_source_update_issue.md
watch_heartbeat_issue.md

# 의존성·운영 잔재물
node_modules/
dist/

# Claude Code 로컬 자산 — 작업방(worktree)이 이 밑에 통째로 생기므로 반드시 제외
.claude/worktrees/
.claude/settings.local.json
.omc/
.serena/
__pycache__/
.pytest_cache/
.ruff_cache/
.playwright-mcp/
supabase/.temp/

# Playwright 산출물
test-results/
playwright-report/
blob-report/
.vercel
