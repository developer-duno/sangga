/**
 * 서버(Supabase)에 있는 것들의 **이름과 짝수(數)** 를 한곳에 모은 곳.
 *
 * 이 파일은 일부러 **아무 일도 하지 않는다** — 읽어도 클라이언트를 만들지 않고
 * 환경변수도 보지 않는다. 그래서 테스트가 이 모듈을 가짜로 바꿀 필요가 없다.
 * (예전에는 이 상수들이 `supabase.ts` 안에 있었다. 그 파일은 읽는 순간 키를 요구하며
 *  throw 하므로 테스트마다 모듈을 통째로 흉내 내야 했고, 흉내 안에 상수를 **거울처럼
 *  다시 적어** 두었다. 진짜 값이 바뀌어도 흉내는 옛 값을 말하니 테스트만 초록인
 *  상태가 만들어진다 — 그 구조를 없애려고 순수 모듈로 갈라 두었다.)
 */

/** 층별 스택의 본체. */
export const FLOOR_STACK_VIEW = 'v_floor_stack';

/**
 * 화면 각주 숫자(점포 수·층 결측 비율)의 출처.
 *
 * 각주를 손으로 박아 두면 새 분기를 적재하는 순간 화면만 옛 숫자를 말한다
 * — 코드는 한 줄도 안 고쳤는데 거짓말이 시작되는 종류의 결함이다.
 */
export const COVERAGE_STATS_VIEW = 'v_coverage_stats';

/**
 * 건물 검색(`{ q, lim, sigungu }` 셋을 받는다).
 *
 * ⓘ 이름에 스키마를 안 적는다 — 클라이언트가 `db: { schema: 'api' }` 로 만들어져 있어
 *   이 이름은 `api.search_buildings` 래퍼로 간다(`lib/supabase.ts`). 형제 상수들과 같은 규칙.
 * ⚠️ 고른 구 안에서만 찾는다(`sigungu` 는 5자리). 같은 건물 이름이 여러 구에 겹치므로
 *    구가 정해지지 않으면 화면이 서버를 아예 부르지 않는다.
 */
export const SEARCH_BUILDINGS_FN = 'search_buildings';

/**
 * 0건이 나왔을 때 **왜 0건인지**를 한 번 더 묻는 함수(`{ q, sigungu }`).
 *
 * ⓘ 위와 같은 이유로 이름만 적는다(`api.search_scope` 로 간다).
 * ⛔ 0건일 때만 부른다 — 평소 검색을 두 번 왕복시키지 않기 위해서다. 돌려주는 `too_broad`
 *    가 참이면 "없는 것"이 아니라 "너무 넓어 서버가 끊은 것"이라 화면 문구가 갈린다.
 */
export const SEARCH_SCOPE_FN = 'search_scope';

/**
 * 지금 **열려 있는 구** 목록(인자 없음).
 *
 * ⓘ 위와 같은 이유로 이름만 적는다(`api.list_open_sigungu` 로 간다).
 * ⛔ 열린 지역의 진실은 **서버 한 곳**이다(`mv_open_sigungu`). 화면에 구 목록을 글자로 박아
 *    두면 자료가 늘어도 화면만 옛 목록을 말한다(2026-08-13 실측 드리프트). `lib/regions.ts`
 *    는 짧은 이름표(서울/대전)를 붙이는 데만 쓴다.
 */
export const OPEN_SIGUNGU_FN = 'list_open_sigungu';

/**
 * 이 건물이 속한 상권을 묻는 서버 함수(`bld_id` 하나를 받는다).
 *
 * 돌려주는 것은 `{ covered, districts[] }` 두 칸뿐이다 — `covered` 는 "그 시·도에
 * 상권 경계 자료가 아예 있는가"이고, `districts` 는 그 건물이 실제로 들어가는 상권들이다.
 * 지역 이름을 화면에 박지 않으려고 `covered` 를 서버가 표에서 직접 센다(자료가 늘면
 * 화면이 저절로 따라온다).
 */
export const BUILDING_DISTRICTS_FN = 'list_building_districts';

/**
 * 이 필지에서 일어난 실거래 이력(`pnu` 하나를 받는다). Stage A — 결정 0012.
 *
 * 지번이 공개된 구간(2024-01 이후)만 나온다. 자기 실거래가 있는 필지는 서울 1.8% ·
 * 대전 1.4% 뿐이라 **대부분의 건물에서 빈 배열**이 정상이다.
 */
export const PARCEL_TX_FN = 'list_parcel_transactions';

/**
 * 이 필지 거래 목록을 서버가 끊는 건수. **서버가 정본**이다
 * (`supabase/schema.sql` 의 `list_parcel_transactions` → `limit 100`).
 *
 * 한 필지에 852건인 곳이 실재해서 서버가 끊는다. 화면은 "잘렸다"고 말해야 하는데,
 * 그 숫자를 따로 박아 두면 서버가 200으로 늘리는 날 화면만 100이라 말한다.
 * ⚠️ 서버 쪽 `limit` 을 고치면 이 값도 같은 커밋에서 함께 고친다.
 */
export const TX_LIST_CAP = 100;

/**
 * 이 필지 거래 목록이 시작되는 달. **서버가 정본**이다
 * (`supabase/schema.sql` 의 `list_parcel_transactions` → `contract_ym >= '202401'`).
 *
 * 그 전 계약분은 지번이 가려져 필지에 붙일 수 없다. 위와 같은 이유로 짝을 맞춰 둔다.
 */
export const TX_OPEN_SINCE_LABEL = '2024년 1월';

/**
 * 실거래 자료에서 층 표기가 빈 값으로 오기 시작한 해.
 *
 * 우리 파싱 문제가 아니라 국토부가 그렇게 준다(원본 전수 재계수로 확인). 층 미상이
 * 왜 이렇게 많은지, 지하는 왜 참고 시세를 못 내는지 두 화면이 같은 사실을 말하므로
 * 숫자를 한 곳에 둔다.
 */
export const TX_BASEMENT_MISSING_SINCE = 2017;

/**
 * 구(시군구) 실거래 단가 분포(`sigungu` 5자리를 받는다). Stage A — 결정 0012.
 *
 * 층대 5칸이 항상 같은 순서로 온다. 구 이름·집계 창도 서버가 함께 주므로 화면에
 * 지역명이나 "최근 24개월" 같은 기간을 글자로 박지 않는다.
 */
export const SIGUNGU_TX_STATS_FN = 'get_sigungu_tx_stats';

/**
 * 이 필지의 층별 참고 매매 시세 밴드. Stage B — 결정 0013.
 *
 * ⚠️ **인자 이름이 `p_pnu` 다.** 형제 넷은 `bld_id`·`pnu`·`sigungu` 인데 이 함수만
 *    접두사가 붙어 있다(파라미터가 컬럼명 `pnu` 와 겹치면 SQL 안에서 어느 쪽인지
 *    갈리지 않기 때문). 목(mock)은 인자 **이름**을 안 보므로 `{ pnu: … }` 로 잘못
 *    불러도 vitest·E2E 는 전부 초록이고 **라이브에서만** PGRST202 가 난다.
 *
 * 돌려주는 것은 층마다 한 줄, 또는 **`gate_fail` 한 줄뿐**이다:
 *  · 참고 시세를 켜도 되는 구인지는 서버가 표(`price_gate_sigungu`)를 보고 정한다.
 *    아니면 층 목록 대신 `floor_no: null` 인 `gate_fail` 한 줄만 온다.
 *  · ⛔ 통과 구 목록을 화면·문서에 복사하지 않는다(결정 0013 §4). 목록의 진실은
 *    서버 한 곳이고, 성적표를 다시 뽑으면 화면이 저절로 따라온다.
 *  · 안 내는 이유가 넷(`gate_fail`·`floor_1f`·`no_evidence`·`no_estimate`)이고 각각
 *    다른 뜻이다 — 화면이 하나로 뭉뚱그리면 "모르는 것"을 "없는 것"이라 말하게 된다.
 */
export const PRICE_BANDS_FN = 'list_price_bands';

/**
 * 이 필지의 층별 국세청 기준시가(㎡당 고시가격의 가운데값).
 *
 * ⚠️ **인자 이름이 `p_pnu` 다**(`list_price_bands` 와 같은 이유 — 컬럼명 `pnu` 와 겹친다).
 *    목(mock)은 인자 **이름**을 안 보므로 `{ pnu: … }` 로 잘못 불러도 vitest·E2E 는 전부
 *    초록이고 **라이브에서만** PGRST202 가 난다.
 *
 * ⛔ 돌려주는 것은 **시세가 아니라 세무 기준가격**이다 — 참고 시세 밴드와 다른 자로 잰
 *    다른 값이라, 화면에서 한 줄에 섞거나 서로 견주는 문구를 쓰지 않는다(`types.ts` 의
 *    `BasePrice` 주석과 한 쌍이다). 서버가 '상가' 종류만 모아 층마다 가운데값을 낸다.
 * ⓘ 자료가 없는 필지는 **빈 배열**이 정상이고, 그때 화면은 그 줄만 조용히 생략한다.
 */
export const BASE_PRICES_FN = 'list_base_prices';

/**
 * 이 필지 둘레의 업종 분포(대분류). 결정 0014 — 상권 지표 1단계.
 *
 * ⚠️ **인자 이름이 `p_pnu` 다**(`list_price_bands` 와 같은 이유 — 컬럼명 `pnu` 와 겹친다).
 *    목(mock)은 인자 이름을 안 보므로 `{ pnu: … }` 로 잘못 불러도 vitest·E2E 는 전부
 *    초록이고 **라이브에서만** PGRST202 가 난다.
 *
 * 돌려주는 것은 두 스코프다 — 속한 상권마다 한 묶음(겹치면 전부) + 반경 하나.
 * 기준 분기·반경 길이도 함께 오므로 화면에 '2026년 6월'·'500m'를 글자로 박지 않는다.
 */
export const INDUSTRY_MIX_FN = 'list_industry_mix';

/**
 * 고른 대분류 안의 중분류 분포. 대분류를 누를 때만 부른다.
 *
 * ⚠️ 인자가 **둘**이다 — `{ p_pnu, p_cat }`. 응답의 `cat_l_cd` 는 물어본 값을 그대로
 *    되돌려 준 것이라, 그 사이 다른 업종을 고른 경우 늦게 온 답을 버리는 데 쓴다.
 */
export const INDUSTRY_DETAIL_FN = 'list_industry_detail';

/**
 * 이 필지 둘레의 **미준공 상업 계열 건축 인허가** 건물 수. 업종 분포 카드 안의 한 줄.
 *
 * ⚠️ **인자 이름이 `p_pnu` 다**(`list_price_bands` 와 같은 이유 — 컬럼명 `pnu` 와 겹친다).
 *    목(mock)은 인자 이름을 안 보므로 `{ pnu: … }` 로 잘못 불러도 vitest·E2E 는 전부
 *    초록이고 **라이브에서만** PGRST202 가 난다.
 *
 * ⓘ 둘레의 범위는 **업종 분포와 같은 기준**이라 화면이 다시 정하지 않는다. 돌려주는 것은
 *   `{ total_cnt, started_cnt, base_ym }` **한 행**이고, 기준월도 함께 오므로 화면에
 *   '2026년 7월'을 글자로 박지 않는다.
 * ⓘ 자료가 없거나(`total_cnt: 0`) 함수가 아직 없으면(PGRST202) 화면은 **그 줄만** 생략한다
 *   — 카드의 나머지는 그대로 선다(업종 분포와 같은 규칙).
 */
export const NEARBY_PERMITS_FN = 'count_nearby_permits';

/**
 * 이 시·도에서 지금 열려 있는 LH 상가 분양·임대 공고.
 *
 * ⚠️ **인자 이름이 `p_sido` 이고, 값은 시군구 코드가 아니라 시도 코드 2자리다**
 *    ('11' 서울 · '30' 대전 — 고른 구 코드의 앞 두 자리).
 *    ⓘ 서버는 5자리를 받아도 앞 2자리만 보게 만들어져 있다(그쪽의 방어선). 그렇다고
 *      **뜻이 다른 값을 보내 놓고 서버의 너그러움에 기대지 않는다** — 계약은 두 자리이고,
 *      그 자름이 서버에서 사라지는 날 화면만 조용히 빈손이 된다(에러가 아니라서 아무도
 *      모른다). 화면 쪽 자르는 규칙은 `lib/lhNotices.ts` 의 `sidoOf` 한 곳에 있다.
 *
 * ⓘ 거르기·정렬은 서버가 한다 — 마감 지난 공고를 빼고, 그 시도 공고 + 전국 공고를 마감
 *   임박순으로 준다. 화면에서 다시 거르지 않는다(같은 규칙이 두 곳에 있으면 갈린다).
 * ⓘ 자료가 없거나 함수가 아직 없으면(PGRST202) 화면은 **카드를 통째로 생략**한다 —
 *   "공고 없음"이라 적으면 모르는 것을 없는 것이라 말하게 된다(업종 분포와 같은 규칙).
 */
export const LH_NOTICES_FN = 'list_lh_notices';

/**
 * 이 필지가 속한 상권의 **부동산원 임대 조사값**. 결정 0024.
 *
 * ⚠️ **인자 이름이 `p_pnu` 다**(`list_price_bands` 와 같은 이유 — 컬럼명 `pnu` 와 겹친다).
 *    목(mock)은 인자 이름을 안 보므로 `{ pnu: … }` 로 잘못 불러도 vitest·E2E 는 전부
 *    초록이고 **라이브에서만** PGRST202 가 난다.
 *
 * ⛔ 돌려주는 것은 **추정이 아니라 조사값**이다 — 곱하지도 나누지도 층으로 펴지도 않은
 *    공표값 그대로다(그래서 층별효용비율은 아예 안 온다). 참고 시세 밴드와 한 줄에 섞거나
 *    서로 견주는 문구를 쓰지 않는다.
 * ⓘ 줄이 **하나도 없는 것이 정상**이다 — 부동산원 표본이 닿는 상권이 훨씬 적다. 그때
 *   화면은 카드를 세우되 "조사 대상 상권이 아니다"라고 적는다(시·도 평균으로 안 메운다).
 * ⓘ 함수가 아직 없으면(PGRST202) 카드를 통째로 생략한다 — 모르는 것을 없는 것이라 말하지
 *   않기 위해서다(LH 공고 카드와 같은 규칙).
 */
export const RENT_STATS_FN = 'list_rent_stats';

/**
 * 화면에서 온 짧은 글 한 통을 창고에 넣는 서버 함수. 2026-08-24b.
 *
 * ⚠️ **인자 이름이 `p_kind`·`p_body`·`p_context` 다**(`list_price_bands` 와 같은 이유 —
 *    `kind`·`body` 는 흔한 컬럼명이라 접두사를 붙였다). 목(mock)은 인자 **이름**을 안 보므로
 *    `{ kind: … }` 로 잘못 불러도 vitest 는 초록이고 **라이브에서만** PGRST202 가 난다.
 *
 * 돌려주는 것은 `true`(넣었다) 또는 `false`(모양이 아니거나 분당 상한에 걸렸다) 하나뿐이다.
 * 예외를 던지지 않는 이유는 이 길로 **오류 보고도 지나가기** 때문이다 — 오류를 알리다
 * 오류가 나면 안 된다.
 *
 * ⛔ 읽는 함수는 없다. 넣은 사람도 자기 글을 다시 못 본다(표가 anon 에게 통째로 닫혀 있다).
 */
export const SUBMIT_FEEDBACK_FN = 'submit_feedback';

/**
 * 의견 본문 글자 수 상한. **서버가 정본**이다
 * (`supabase/schema.sql` 의 `submit_feedback` → `left(v_body, 2000)` + 표 CHECK 제약).
 *
 * 서버는 넘치면 **자른다**(거절이 아니라 — 긴 의견을 통째로 버리는 것이 더 나쁘다).
 * 화면이 미리 막아 주는 편이 친절하므로 같은 값을 쓰되, 서버 쪽을 고치면 이 값도
 * 같은 커밋에서 함께 고친다.
 */
export const FEEDBACK_MAX_LEN = 2000;

/**
 * 상권 하나에 속한 **땅(필지)** 목록. 결정: Wave 3 상권 → 건물 다리(2026-08-31b).
 *
 * ⚠️ **인자 이름이 `p_district_id`·`p_limit`·`p_offset` 이다**(`list_price_bands` 와 같은
 *    이유 — `district_id` 는 컬럼명과 겹친다). 목(mock)은 인자 **이름**을 안 보므로
 *    `{ district_id: … }` 로 잘못 불러도 vitest·E2E 는 전부 초록이고 **라이브에서만**
 *    PGRST202 가 난다.
 *
 * ⛔ 한 줄은 **건물이 아니라 땅**이다. 점포 수를 필지 단위로만 셀 수 있어서다
 *    (`unit_business.unit_id` 295만 행 전량 NULL). 건물로 줄세우면 한 땅의 동들이 같은
 *    점포 수를 **복사해** 갖는다 — 명동 상위 10에 `롯데호텔 및 백화점 317` 이 네 줄
 *    연달아 나온다. 그래서 서버가 땅으로 묶어 대표 동 하나만 싣고, 몇 동인지를
 *    `bld_cnt_in_pnu` 로 알린다.
 * ⓘ 잘리기 전 전체 규모(`total_parcel_cnt`·`total_bld_cnt`)가 모든 행에 같은 값으로 온다
 *   — "몇 곳 중 몇 곳"을 정직하게 말하려면 필요하다(검색의 `total_cnt` 와 같은 수법).
 * ⓘ 함수가 아직 없으면(PGRST202) 화면은 **목록을 통째로 생략**한다(LH 공고와 같은 규칙).
 */
export const DISTRICT_BUILDINGS_FN = 'list_district_buildings';

/**
 * 한 땅에 선 **동 목록**. "같은 땅에 N동"을 펼칠 때만 부른다.
 *
 * ⚠️ 인자 이름이 `p_pnu` 다(형제들과 같은 이유).
 *
 * ⓘ 목록에 동을 미리 실어 두지 않는 이유: 필지의 94%(176,488/188,442)가 1동뿐이라
 *   대부분 쓰이지 않는 짐이 된다. 대전역 상권에서는 50곳에 606동이 딸려 온다.
 * ⓘ `dong_nm` 을 함께 주는 이유: 한 땅의 동들은 **건물명이 같은 일이 흔하다**
 *   (롯데호텔 4동 전부 '롯데호텔 및 백화점'). 이름만으로는 무엇을 고르는지 알 수 없다.
 */
export const PARCEL_BUILDINGS_FN = 'list_parcel_buildings';

/**
 * 상권 건물 목록을 한 번에 몇 곳씩 받아 올지. 👤 사장님 결정(2026-08-31): "우선 50개 + 더 보기".
 *
 * ⚠️ 서버가 상한 200 으로 한 번 더 막는다(`greatest(1, least(coalesce(p_limit,50), 200))`).
 *    이 값을 200 위로 올리면 **에러 없이 조용히 200 으로 깎인다** — 화면만 더 받았다고
 *    믿게 되므로, 늘리려면 서버 쪽 상한도 같은 커밋에서 함께 올린다.
 */
export const DISTRICT_BUILDINGS_PAGE = 50;

/**
 * 화면 아래 "이 자료는 언제 것인가" 표. 2026-09-05d.
 *
 * ⓘ **인자가 없다** — 이 표는 무엇을 보던 중이든 같다(형제들과 달리 `p_pnu` 가 없다).
 *
 * ⛔ 돌려주는 것에 **화면이 아는 값이 하나도 없다.** 자료 이름·기준 종류·기준값·다음 갱신
 *    예정일·주기까지 전부 서버가 준다. 신선도를 화면에 글자로 박아 두면 적재하는 순간부터
 *    그 글자만 거짓말을 한다 — 코드는 한 줄도 안 고쳤는데 화면이 옛 분기를 말하는,
 *    이 레포가 가장 무서워하는 종류의 결함이다(각주 숫자를 `v_coverage_stats` 로 옮긴
 *    것과 같은 이유).
 * ⓘ `basis` 가 null 인 줄이 와도 **그 줄은 그린다**(화면이 '자료 없음'이라 적는다).
 *   줄을 빼면 "그런 자료를 안 쓴다"로 읽힌다.
 * ⓘ 함수가 아직 없으면(PGRST202) 화면은 **표를 통째로 생략한다** — 아래 안내는 그대로
 *   선다(LH 공고 카드와 같은 규칙).
 */
export const DATA_FRESHNESS_FN = 'get_data_freshness';
