# 알려진 한계 (기준선)

> **이 문서의 목적**: 매 세션 같은 것을 다시 발견하고 다시 조사하는 낭비를 막는다.
> 여기 있는 것은 **"이미 아는 것"** 이다. 새 세션은 이 목록을 먼저 읽고, 여기 없는 것만 조사한다.
>
> 담는 것 = **사실이지만 지금 구조상 어쩔 수 없거나, 나중에 손봐야 하는 것.**
> 담지 않는 것 = 고칠 수 있는 결함(그건 `PROGRESS.md`의 "다음 단계"로 간다).
>
> 최초 작성 2026-08-08 (3차 적대검증 legacy-limits 관점, 23개 항목 전수 사실 확인).
> **새 한계를 발견하면 여기 추가하고, 해소되면 취소선 처리 후 해소 근거를 남긴다.**

---

## 1. 데이터 원천 자체가 없는 것 🔒 (해소 불가)

| 한계 | 근거 | 지금 영향 |
|---|---|---|
| **상가 임대료 실거래가 존재하지 않는다** | 신고 의무 없음. `CLAUDE.md` 절대 규칙 5 | 임대료는 부동산원 수익률·층별효용비율로 **역산**할 수밖에 없다. 추정임을 반드시 명시 |
| **권리금 공적 데이터 없음** | `상세계획.md` §3.3 | 상가 가치의 큰 축이 빠진다 |
| **상가 실매출은 서울만 있다** | `상세계획.md` §3.3 | 비수도권 리포트는 항목이 줄어든다(§3.2 리포트 이원화의 이유) |
| **통건물(일반건축물) 실거래는 지번이 항상 마스킹** | 실거래 PNU 조립 성공률 **0%**(구조적) | 통건물은 필지에 못 붙인다. 지역 단가에만 쓴다 |
| **실거래 지번은 2024-01부터만 공개** | 2023-12까지 100% `'6**'` 마스킹. 2024-01에 68.6%로 급변(제도 변경) | 과거 백필(54,000콜)은 PNU를 못 붙이는 데이터를 받는 작업 → 우선순위 낮음 |
| **상권정보에 호(戶) 정보가 전수 결측** | `unit_business.ho`·`unit_id` 채움률 **0%**(64,239행) | 점포를 호실에 못 붙인다. (PNU, 층)으로만 붙는다 → 스택뷰가 D등급인 근본 이유 |
| **LH 상가 공고의 호실 목록·가격은 API 가 안 준다** | 공급정보 API 실측(2026-08-27, 분양 입찰·임대 추첨 각 1건): 상가 공고는 빈 응답, 주택 공고는 스키마까지 반환 — 상가만 미제공 | 알림판(결정 0022)은 공고 메타(제목·유형·마감·상태)까지만 담고, 상세·신청은 LH 청약센터 링크로 넘긴다. 지역도 시도 단위뿐이라 구 단위로 못 좁힌다 |
| **층별효용비율이 7구간뿐 — 지하2층 이하·옥탑은 아예 없다** | R-ONE 실호출 실측(2026-08-08): 층 축이 `지하1층·1~5층·6층이상` 7개. ★ **구간 실측 보강(2026-08-09 전 표 스팟체크)**: 이 7구간은 상가 3종 기준이다. **오피스 표만 8구간**(`6층이상` 대신 `6-10층`·`11층이상`)이라 현행 7구간 매핑에서 **오피스 6층 이상 효용비율은 적재되지 않는다**(기록만 방침 확정 — raw에 보존되므로 매핑 확장 후 재적재로 복구 가능). **소규모상가는 3구간**(지하1·1·2층)만 존재(저층 건물 정의상 자연스러움 — 적재 채움 35.3%가 낮은 이유). 우리 `building_floor` **135,619행**(3일차 완주 후 2026-08-09 재계산) 대조 시 개별 계수 적용 가능 **66.0%**, 6층 이상은 뭉친 값 **21.5%**, **계수 없음 12.6%**(지하2층 이하 7.8% + 옥탑 4.8%) | §6.1 임대 역산 공식이 12.6%에서 성립하지 않는다. ✅ **방침 확정 (2026-08-09)** — `docs/decisions/0001` **A안 승인**: 해당 층은 추정 숫자를 계산하지 않고 "근거 데이터 없음"으로 표시한다. 없는 계수를 이웃 층에서 만들어 쓰는 코드는 만들지 않는다(절대 규칙 3의 근거가 사라지므로) |
| **2017년부터 상업업무용 실거래에 지하층이 없다** | 원본 JSONL 23,599건 전수 재계수(2026-08-08). 구분소유 거래의 지하 건수: 2014년 209 · 2015년 347 · **2016년 51** · **2017년~2026년 전부 0**. 우리 파싱 문제가 아니다 — 국토부 API가 `floor` 칸을 빈 값으로 준다. 사라진 것이 아니라 **층 결측으로 흡수**됐다(집합거래 결측 비율 2015년 22% → 2017년 이후 35~49%) | ① 지하 시세를 실거래(A등급)로 못 받친다. R-ONE 계수도 지하2층 이하는 없으므로 **`building_floor`의 지하 21.4%(지하1층 13.6% + 지하2층 이하 7.8%)는 근거가 통째로 없다**(135,619행 기준 2026-08-09 재계산). ② 더 위험한 것: **층 결측이 무작위가 아니라 지하 쪽으로 치우쳐 있다** — 결측을 지역 평균으로 메우면 지하가 지상 가격으로 오염된다 |
| **R-ONE의 지역 축이 시군구가 아니라 상권이다** | `GRP_FULLNM`이 `서울>강남>테헤란로` 형태(서울 45개 상권). PNU로 상권을 특정할 수단이 없다. 같은 강남 안에서도 지하1층 효용비율이 테헤란로 **22.7** vs 강남 상위 **40.9**로 **1.8배** 차이(2026Q2 실측) | 상권 경계 데이터가 없으면 상위 레벨(`서울>강남`)로 근사할 수밖에 없고, 그 사실을 화면에 명시해야 한다. 어느 상권을 고르냐가 임대료 추정을 배 단위로 흔든다. 🔶 **2026-08-14 부분 완화**: `district` 에 서울 상권 경계 1,650개가 들어와 이제 PNU→상권 특정은 된다(`st_contains`). **그러나 두 자료의 상권 이름 체계가 다르다** — R-ONE 서울 상권명 72개 중 `district` 와 **정확히 일치 20개(27.8%)**, 부분 일치까지 61개(라이브 실측). 즉 R-ONE 상권명 ↔ district 매핑표를 만들기 전에는 여전히 근사였다. ✅ **매핑표 완성(2026-08-14 오후)** — `district_rone_map` 396행(상권 376곳 ↔ R-ONE 잎 **85/88**, 서울 381·대전 15). 이름 규칙+권역 관문+지리 큐레이션, 랜덤 대조 5.1배로 판별력 검증. ⚠️ 남는 근사 2가지: ①미매핑 잎 3개(관평동·대전동구청·양재말죽거리)와 매핑 안 된 상권 1,311곳(골목상권 대부분 — R-ONE 이 애초에 안 다루는 좁은 상권)은 **상위 근사 유지** ②같은 상권이 bld_type 마다 경로가 다른 이중 권역(공덕역·당산역·여의도·홍대/합정)은 두 경로를 다 넣어 해소 |

## 2. 지금 데이터 상태의 한계 (해소 가능하나 시간·수집이 필요)

| 한계 | 실측값 | 조건 |
|---|---|---|
| **실거래는 PNU(필지)로만 붙는다 — 건물키(bld_id) 연결은 없다** | `transaction` **44,052행**(서울+대전 24개월 창, 2026-08-15 재수집). 집합거래 PNU 조립 **99.7%(서울)/100%(대전)**·parcel 매칭 90.0%. 일반(비집합) 거래는 지번이 마스킹돼 PNU 원천 불가. 층·단가까지 갖춰 사다리에 쓰이는 것은 **10,216건**(성적표 v1) | 화면·시세 함수는 전부 PNU 경로 사용(문제 없음). ⚠️ 옛 수치(22,662행·PNU 7.8%)는 2026-08-08 기록 — 다시 인용하지 말 것 |
| **호실(unit)은 강남 건물의 18.35%에만 존재** | 1,083 / 5,903 — ⚠️ **분모가 1일차 적재분이다**(3일차 완주 후 building = **13,817행**). 비율 자체는 재계산 전까지 "1일차 5,903행 시점 기준"으로 읽을 것. 전유공용면적은 집합건물에만 있다 | 그래서 스택뷰를 층 축으로 재설계했다(§8.6). 호실 축으로 되돌리지 말 것 |
| **층 결측이 무작위가 아니다** | 전국 54.75%. 서울 37.33% ~ 경북 71.91%, 군 단위 90%대, 숙박업 87.9% | 파일럿(강남) 성적이 전국을 대표하지 않는다. 백테스트에 비수도권 대조군 필요 |
| ~~**미수집 941 PNU가 3개 동에 100% 몰려 있다**~~ | ~~도곡 572/572 · 일원 295/298 · 수서 74/74~~ | ✅ **해소 (2026-08-09)** — 3일차 수집 완주(941/941, 실패 0). 분리 집계 결과 도곡 97.03%·일원 98.99% 통과, 수서만 PNU 기준 86.49%(원인은 아래 행) |
| **수서동 10개 필지는 건축HUB에 표제부가 아예 없다** | 2026-08-09 실측: 수집 성공했는데 API가 빈 응답(raw에 문자열로도 0회, 대조군 정상 필지 193줄). 점포 **64개**(수서의 6.5%). ⛔ **"대표지번에 숨었다" 가설도 반박됨** — 64개 점포의 도로명주소를 강남 전체 대장(12,274필지 수집분)의 `newPlatPlc`와 전수 대조 시 일치 **0/64**(대조군인 정상 수서 점포는 20/20 일치 = 방법 유효). 즉 도로명주소 2차 조인으로도 못 붙인다. 군집: 밤고개로 99(18)·광평로 196(13)·광평로 270(10), 업종 1위 식물 소매(12) — 화훼단지형 간이 건물 **추정**(미확인). 상권정보 자체 층 보유 16/64(25%)뿐 | 이 점포들은 층 스택에 못 붙는다(**원천 부재 확정** — 코드로 해소 불가). 수서동 PNU 조인률 86.49%의 전부가 이것. §5.6 3분류상 "③ API 무데이터"라 로직 재점검 대상 아님. 대응: ① 현 화면에선 건물이 없어 검색에 아예 안 나옴(빈 층으로 오표시되지는 않음) ② 지도/필지 화면(Phase 5+) 만들 때 "대장 없는 필지 — 점포 N개" 목록 표시(D등급) ③ 브이월드 키 수령 후(어차피 도로접면 때문에 예정) 건물통합정보류 데이터에 이 10개 필지가 있는지 1회만 확인 |
| ✅ **절단 PNU 19개 — 일괄 파일로 해소 (2026-08-11 실측)** | 건축HUB **일괄 파일에는 1만 행 상한이 없다.** 강남 전유공용을 API 수집분과 대조: API 1,069,166행 → 일괄 **1,177,888행**(+10.2%). 그 차이의 **94.0%(+102,212행)가 정확히 그 19개 PNU의 절단 회복분**이다(최악 사례 `1168010300112840000`: 10,000 → **30,935행** — API로는 3분의 1만 받고 있었다). 나머지는 일괄에만 있는 PNU 57개(6,510행). ⭐ **API 에만 있는 PNU는 0개** = 일괄 파일이 API의 **상위집합**이다(손실 없음) | ⇒ **결정 0002(A안: 재수집 포기·D등급 배지)의 전제가 사라졌다.** 일괄 파일로 적재하면 배지가 필요 없다. 표제부·층별개요도 같은 방향(+405동·+2,604행). 재검토 방아쇠: [C] 적재 완료 시 `docs/decisions/0002` 갱신 |
| **절단 PNU 19개** — 전유공용면적이 1만 행 상한 초과 (⚠️ 위 줄로 해소됨 — 아래는 API 경로 기준 옛 서술) | 전부 대형 아파트. `numOfRows`(100) × `MAX_PAGES`(100) = 1만. ★ 3일차 완주로 13→**19개**(수서·도곡·일원 대형 아파트 6개 추가, 2026-08-09 적재기 재계수) | ✅ **방침 확정 (2026-08-09)** — `docs/decisions/0002` **A안 승인**: 재수집하지 않고(API 예산 0) 화면에 "층별 상세 일부 누락" D등급 배지만 붙인다. 점포 목록(`unit_business`)은 별개 파이프라인이라 정상 표시된다. 적재기는 실행 시점에 다시 세므로 코드는 자동 대응 |
| **2017Q3 분기 파일만 구조가 다르다** | 전국 4분할·업종 대분류 8종의 구 체계 | 시계열 정제 시 전용 파서 또는 제외. 미구현 |
| **`unit` 행 수 ≠ 물리적 호실 수** | 복층 1호실이 층별 2행(63,496 → 63,717) | "호실 수" 집계는 `(bld_id, ho)` DISTINCT 필수 |
| ✅ **(해소) 광주·전남 행정 개편이 화면 목록에 반영됐다** | 2026Q2부터 광주·전남이 **행정 개편**으로 합쳐졌다(단순 파일 합본이 아니다 — §3.4·§상세계획 정정 참조). 라이브 `bjd_code` 실측(2026-08-13): 시도코드 `12`(`전남광주통합특별시`)가 활성 **3,204행**, 옛 `29`(광주광역시)·`46`(전라남도)는 **`is_active=false`**(246·4,821행). `parcel`도 이미 `12`로 들어온다(88,460행, `29`·`46`은 0행) | **PR #46에서 정정 완료** — `src/lib/regions.ts`가 17개 → **16개**(`29`·`46` 제거, `12` 추가). 다만 이 목록은 이제 **화면 표시용이 아니다**: 시도 칩은 서버 `list_open_sigungu()`가 실제 건물 수로 만들고, `regions.ts`는 이름 표기(코드→한글)와 적재 범위 판단에만 쓰인다. 즉 이 표가 다시 낡아도 화면은 안 틀어진다 |

## 3. 화면이 보여주는 값의 한계 (사용자가 오해할 수 있는 것)

| 한계 | 실측값 | 화면 대응 |
|---|---|---|
| **`store_cnt`를 밀집도로 쓰면 안 된다** | 이상 층 305개, 영향 점포 **약 12.3%**. 원인 1순위 = 한 필지 복수 동 중복(72.5%), 2순위 공유오피스 | D등급 배지 + 빠짐·겹침 양방향 경고 상시 노출. **경쟁밀도·상권 DNA 계산 전에 반드시 걸러야 한다** |
| **점포가 3곳 중 1곳 빠진다** | `unit_business.floor_no` NULL **32.9%** — 층이 없으면 어느 층에도 안 붙는다 | 경고 문구로 고지. 근본 해소 불가(원천 결측) |
| **건물 이름이 절반 이상 없다** | 12,405개 중 5,361개(43.2%) → **5,765개(46.5%)**. ⚠️ **분모 12,405는 2일차 시점이다** — 3일차 완주로 building이 **13,817행**(+1,412)이 됐으므로 분모·분자·비율 셋 다 재계수 전까지는 "2일차 12,405행 기준"으로 읽을 것(적대검증 2026-08-10 지적) | 🔸 **부분 해소 (2026-08-08e)** — 건축물대장에는 이름 칸이 둘인데(`bld_nm`·`dong_nm`) 앞의 하나만 보고 있었다. 동명칭에만 이름이 든 **404개**를 되찾았다(일반 라벨 '주건축물제1동' 284개는 걸러냄). 라이브 실증: `노보텔앰배서더서울`(16층)·`WeWork Building`(19층)·`역삼동 718빌딩`(20층)·`강남텔레피아빌딩`(13층)이 **전부 0건**이었다. 나머지 **6,640개(53.5%)는 여전히 이름이 없다** — 도로명 검색이 주 경로인 것은 그대로. ✅ **라이브 적용·검증 완료(2026-08-08)**: 위 4개 검색이 전부 1건으로 바뀌었고 `빌딩` 검색이 1,742 → **1,840건(+98)**, 원본 표 4종은 401 유지 |
| **면적이 없는 층이 약 3%** | 그 층이 전부 연면적 제외분(계단실·물탱크실 등) | 막대를 그리지 않는다(2%짜리 막대가 "아주 좁은 층"으로 오해됨) |
| **건물의 3곳 중 1곳은 어느 상권에도 안 들어간다** — ⚠️ **평가 불가가 아니다** | 서울 건물 **197,403**동 중 **62,169동(31.5%)**이 어떤 `district` 에도 안 잡힌다(2026-08-14 실측). 상권이 서울 면적의 27%(164.9km²)만 덮으니 당연하다 — 주거지·녹지는 애초에 상권이 아니다. **평가 재료는 상권 안팎이 사실상 같다**(라이브 실측: 공시지가·도로접면·용도지역 **99.7% vs 100%**, 층정보 99.8% vs 100%) | 상권은 **비교 축 하나**일 뿐 평가의 전제가 아니다. §6.2 시세 밴드는 **반경 500m 실거래** 기준이라 상권과 무관하고, §6.1 임대 역산은 R-ONE 시군구·상위상권 근사를 쓴다. 상권이 없으면 못 하는 것은 **딱 하나 — 상권 간 유사도 비교**(확정 설계 5번, 8차원 벡터). 화면에서는 "상권 정보 없음"이 **정상 상태**이고, 그 건물의 다른 지표는 그대로 다 보여준다. ⚠️ 이 표를 "상권 없으면 평가 못 함"으로 읽으면 안 된다 |
| **한 건물이 두 상권에 걸치기도 한다** | 서울 **3,342동(1.7%)**이 2개 이상 `district` 에 동시에 들어간다(2026-08-14 실측). 상권 경계가 겹치도록 그려져 있다 | 🔴 **이쪽이 진짜 조심할 것.** `st_contains` 조인이 그만큼 **행을 늘려** 집계에 쓰면 중복 계수된다. 화면에 한 줄만 보여주려면 무엇을 고를지 정해야 한다(면적 최소? 발달상권 우선? 전부 나열?) ~~— 지금 아무 규칙도 없다~~ → ✅ **규칙 확정 (2026-08-15, `docs/decisions/0011`)**: **개별은 전부 나열**(겹치면 겹치는 대로 보여 준다 — 감추면 "왜 저 상권이 안 뜨지"가 된다), **합산에서는 관광특구를 뺀다**(겹침 2,964건의 짝이 전수 관광특구가 낀 경우뿐이었다 — 세밀 유형끼리는 안 겹치게 그려져 있고 관광특구만 그 위에 얹힌다), **관광특구는 합산 축이 아니라 라벨**로 쓴다. ⚠️ 이 단서를 빼고 "확정"이라고만 옮기면 다음 사람이 또 전부 더한다 |
| ~~**대전 5개 구는 상권이 0개다**~~ | ~~소스(서울 상권분석서비스)가 **서울 전용**이다. 우리가 연 30개 구 중 25개만 덮인다~~ | ✅ **해소 (2026-08-14, 결정 0009 · 재확인 2026-09-01)** — 소진공 **주요상권현황**으로 대전 **37개**를 적재해 5개 구가 전부 덮였다(라이브 실측 `district` 총 **1,687개** = 서울 1,650 + 대전 37). 출처가 둘이라 화면은 상권마다 `source_nm` 을 함께 적는다. ⚠️ 이 줄이 2주 넘게 "0개"라고 말한 채 남아 있었다 — 해소한 사람이 이 문서를 안 고쳐서다 |
| ~~**화면 안내의 숫자가 하드코딩**~~ | ~~`32.9%`·`64,239곳`이 `FloorStack.tsx`에 문자열로 박혀 있다~~ | ✅ **해소 (2026-08-08 오후)** — 뷰 `v_coverage_stats`에서 읽어 런타임 계산한다(마이그레이션 `2026-08-08d`). 각주가 `v_floor_stack`과 **같은 분기 기준**을 쓰므로 새 분기를 적재하면 화면이 저절로 따라간다. 회귀 가드 = `FloorStack.test.tsx`("DB 값이 바뀌면 각주도 따라 바뀐다"). 조회 실패 시 옛 숫자로 되돌아가지 않고 숫자만 뺀다(라이브 확인: "적지 않은 수"·"상권정보 최신 분기 기준"). ✅ **마이그레이션 d 라이브 적용·검증 완료(2026-08-08)** — 화면에 `32.9%`·`64,239곳` 표시 확인, 원본 표는 401 유지 |
| **LH 상가 공고 카드에 뜨는 것의 약 90%는 그 지역 공고가 아니다** — ✅ 대응 | 라이브 실측(2026-09-01): 서울에서 **39건** 중 서울 공고는 **4건**, 대전은 **36건** 중 **1건**. 나머지 35건은 `is_nationwide` 로 딸려 온다. ⛔ **`is_nationwide` 의 뜻을 정정한다**(2026-09-05) — 예전에 여기 적어 둔 "LH 지역본부 칸이 비었다"는 **틀렸다.** 수집기 `map_sido` 실측: 빈 칸은 false 로 떨어지고, 참이 되는 것은 LH 가 `CNP_CD_NM` 에 **'전국'이라 글자로 적어 준 경우뿐**이다(결측이 아니라 LH 의 말이다). 다만 그 35건의 제목은 인천계양·부산명지·청주모충처럼 **특정 도시**이고, 진짜 전국인 것은 `LH희망상가 통합공고` 정도다 | ✅ **대응 (2026-09-05c, 결정 0026)** — LH 가 '전국'이라 적은 줄은 **"LH가 지역을 '전국'으로 적은 공고 N건"** 묶음으로 접어 두고(지우지 않는다 — 걸러 내면 서울 카드가 39→4건으로 사실상 빈다) 요약 줄이 그 수를 따로 밝힌다. ⛔ **제목으로 지역을 추측하지 않는다**(지명이 안 든 제목·같은 이름의 다른 동네에서 조용히 틀린다 — 틀린 지역표는 없느니만 못하다) |
| **같은 물건이 여러 줄로 세어진다** — ✅ 대응 | 라이브 실측(2026-09-01): 살아 있는 **64줄**이 서로 다른 물건 기준으로는 **59개**(약 8%가 재게시· [정정공고] 쌍). 카드가 말하는 'N건'이 그만큼 부풀어 있다. 재현: `count(distinct regexp_replace(pan_nm, '\[[^]]*\]|\s+', '', 'g'))` — 대괄호 표시([정정공고] 등)와 공백을 **둘 다** 지운 기준이다(하나만 지우면 60·62 가 나온다). | ✅ **대응 (2026-09-05c, 결정 0026)** — **종류(kind_cd)가 같고** 그 정규화 제목이 같으면 **최신 한 줄만**(공고일→수집시각→pan_id 순) 내보내고 줄에 "정정·재게시 N회"를 적는다. 건수도 묶은 뒤 기준이다. ⛔ 종류를 열쇠에서 빼지 말 것 — 창고 전체 실측이 제목만 **417** vs 제목+종류 **419** 라 같은 제목·다른 종류인 **2쌍**이 합쳐진다(분양과 임대는 다른 물건이다). ⛔ 열쇠를 더 뭉개지도 말 것 — 헐거워지면 서로 다른 공고를 합쳐 **여러 줄로 세는 것보다 나쁘다** |
| **"곧 올라오는 상가 건물"에 이미 실효됐을 허가가 섞인다** — ⚠️ 결재 대기 | 라이브 실측(2026-09-01): 서울 상업계열 미준공 **9,295건** 중 **3,337건(35.9%)** 이 허가 후 **2년 넘게 미착공**이다. 건축법 §11⑦ 은 허가 후 2년(1년 연장 가능) 미착공이면 허가가 실효된다고 본다 — 즉 '곧 올라온다'가 아니라 **안 올라올 수도 있는 것**이 3분의 1이다 | 지금 셈은 `use_apr_day is null`(미준공) + 용도만 본다. ⛔ **LH 마감일과 똑같은 병이다** — 날짜/상태 축을 **하나만** 봤다. 다만 원본에 '실효' 상태 칸이 없어 착공일로 **추정**해야 하므로, 어느 선을 그을지가 결재 영역이다 |
| **"새로 올라오는 상가 건물" 셈은 하한이다** | 인허가 기본개요의 **주용도가 빈 값인 행이 70%**(2026-07판 적재 대상 556,527행 중 389,822행) — 상업 계열(03·04·07·14) 필터가 빈 값을 못 본다. 열면 숫자가 대여섯 배로 부풀어 제외(결정 0023) | 화면 문구가 "허가를 받았다고 모두 지어지는 것은 아닙니다" 한계 부기 포함. 숫자는 "확인된 상업 계열만"이라는 하한으로 읽어야 한다 |
| **국세청 기준시가 줄은 시세 카드가 뜨는 곳에서만 보인다** | 시세 카드는 게이트(결정 0013 — 통과 14개 구)로 통째 숨는 지역이 있고, 기준시가 줄은 그 카드 안에 산다(결정 0021 결정 2, v1 의도된 한계). 공식 고시값이라 게이트와 무관하게 보여줄 수도 있으나 카드 노출 규칙 변경은 별건 결재 사항 | 카드가 뜨면 층별 ㎡당 중앙값 + 집계 호 수 + 고시일 병기, "세무 기준가격 — 시세가 아닙니다" 부기(카드당 1회). 오피스텔만 있는 건물·자료 없는 필지는 줄만 조용히 생략(0원 표시 없음). 함수 부재(PGRST202)에도 카드 나머지는 정상 |

## 4. 인프라·운영의 한계

| 한계 | 근거 | 조건 |
|---|---|---|
| ~~**원본 `data/raw` 15GB에 백업이 없다**~~ | ~~`.gitignore` 대상, git 미추적, 백업 스크립트 0건~~ | ✅ **해소 (2026-08-08 오후)** — `scripts/backup_raw.py`로 외장 SSD(`F:\sangga-raw-backup`)에 백업. **1회 실행 완료: 68개 파일 / 14.3GB, SHA-256 전량 일치**(`--verify`로 백업본을 다시 읽어 대조). 삭제 전파를 막으려 robocopy `/MIR`이 아니라 `/E`를 쓴다(원본을 실수로 지운 뒤 백업이 돌아도 사본이 안 지워진다). ⚠️ **자동 실행이 아니다** — 새 분기를 받으면 사람이 다시 돌려야 하고, F를 꽂아야 실행된다 |
| **분기 수집이 여전히 사람 손이다** | 감시는 생겼으나 받기·적재·백업은 수동 | 🔸 **부분 해소 (2026-08-08)** — `.github/workflows/sangkwon-quarterly-watch.yml`이 매주 포털을 확인해 **새 분기가 뜨면 이슈를 자동으로 연다**(비밀값 0개, 기본 토큰만). 라이브 실호출 확인: 분기 43개 인식, 새 분기 없음. ✅ **GitHub 러너 실행까지 확인**(2026-08-08 07:14Z 수동 1회, run 31245712482 `success` — 로컬에서 도는 것과 러너에서 도는 것은 다른 문제라 눌러 봐야 안다). ⚠️ **받는 것·적재·백업은 여전히 사람이 한다** — 원본 백업이 외장 SSD(F:)라 어차피 사람 손이 필요하고, 적재를 자동화하려면 DB 서비스키를 GitHub에 올려야 한다. 기준선 `LATEST_KNOWN_QUARTER`(현재 `20260630`)를 **적재 후 사람이 올려야** 다음 분기를 제대로 감지한다. 다음 분기 2026Q3은 **2026-10-31** 게시 예정. ★ **2026-08-14 보강 (PR #47)**: 08-10 예약 실행이 `Errno 110`으로 죽었는데 **나흘간 아무도 몰랐다** — 실패하면 이슈가 안 열렸기 때문. ① 목록 조회 **30초 타임아웃 × 3회 재시도** ② **실패해도 이슈를 연다**(`if: failure()`). 일회용 브랜치로 진짜 실패시켜 실전 확인 완료. ~~🔴 **아직 남은 구멍**: "돌았는데 실패"만 알린다 — **예약이 아예 안 도는 경우는 여전히 조용하다**(공개 레포 60일 무활동 시 자동 중지 · 부하 시 큐 드롭, 둘 다 GitHub 공식 문서 명시). 마지막 성공 시각을 기록·판정하는 장치가 없다~~ → ★ **2026-08-22 해소**: `scripts/check_watch_heartbeat.py` 신설 + **상호 감시** 배선. 감시 둘이 자기 일을 마친 뒤 **상대의 마지막 성공 시각**을 GitHub REST API 로 읽어 **8일**(주 1회 예약 + 하루 여유)을 넘으면 이슈를 연다 — 한쪽이 살아 있는 한 다른 쪽의 죽음이 일주일 안에 뜬다. 둘 다 죽는 경우는 사람이 `python scripts/check_watch_heartbeat.py`로 잡는다(무인증·읽기 전용). ⚠️ **판정 기준은 "성공한 실행"이다** — 예약은 도는데 매번 실패하는 감시도 8일 뒤 멈춘 것으로 본다(실패 이슈와 겹쳐 알림이 둘이 될 수 있지만, 일주일 넘게 못 고친 감시는 그럴 만하다). 하트비트 단계에 `if: !cancelled()` 를 붙여 **내 본연의 감시가 실패한 주에도 상대는 본다**. 🔸 **아직 남는 구멍 1개**: **조회 실패(종료코드 2)는 이슈를 열지 않으므로** 지속적 조회 실패(워크플로우 파일명 드리프트 → 404, `actions: read` 권한 회수 → 403)는 여전히 조용하다 — **파일명 드리프트는 pytest 가 잡고**, 나머지는 **로컬 점검 명령**이 보완한다. ★ **2026-08-24·08-28 확장 (기록 2026-09-01)**: 그물이 **다섯**이 됐고(분기 스냅샷·상권 원천·라이브 생존·의견함 주간 알림·LH 공고) **각자가 나머지 넷을 전부** 본다. 기준일도 하나가 아니다 — 주 1회짜리는 8일, **6시간마다 도는 라이브 생존 감시는 1일**이다(감시의 감시가 감시보다 굼뜨면 뜻이 없다). 위 "8일"은 감시가 둘이던 2026-08-22 당시의 값이다 |
| ~~**프론트 테스트 러너가 없다**~~ | ~~vitest·playwright 미설치. CI `web` job은 lint+build만~~ | ✅ **부분 해소 (2026-08-08 오후)** — vitest + jsdom + @testing-library/react 도입, **51개** 테스트, CI `web` job에 `pnpm test` 추가. 옛 결함을 일부러 되살려 테스트가 실제로 빨간불이 되는지 확인했다(층 표기 2건·선택 잔존 1건). ✅ **완전 해소 (2026-08-08, PR #28)** — playwright E2E 도입 완료(`e2e/floor-stack.spec.ts`, CI `web` job에서 chromium으로 실행). ⚠️ 이 줄은 2026-08-10까지 "여전히 없다"로 남아 있어 조사 에이전트가 같은 조사를 반복하게 만들었다 — **해소한 항목은 그 자리에서 갱신할 것** |
| **⚠️ [C] 용량 전망의 강남 편향 — 정량화됨 (2026-08-10 2차)** | [C] 건축HUB 전국 용량(+약 15GB)은 **강남 × 89.59배** 단일 계수에 기댄다. 그 배율이 과대임을 두 갈래로 확인: ① **밀도 실측** — 상권정보 Q1 전수 재계수로 강남 **5.23 점포/필지** vs 전국 평균 **2.48**(강남이 **2.11배** 고밀). 최저는 경북 1.74(강남의 33%), 최고는 세종 3.75 ② **전국 총량 상한** — 강남 우리 필지의 전유공용 1,069,166행에 89.59를 곱하면 9,579만 행인데, 이는 **전국 전유공용 총량 1억947만 행의 87.5%**다. 우리 필지는 상업 필지 110만 개뿐이고 전국 전유부의 대부분은 아파트이므로 **성립할 수 없다** | ⇒ **[C] +15GB는 과대추정 쪽**이다(약점 ① 할당 디스크 과소와 반대 방향이라 일부 상쇄). 점포 배율(42.39배)로 곱하면 41.4%로 떨어져 그럴듯해진다. ⛔ 단 `unit_business`(점포) 전국 행 수는 **이미 직접 세었으므로**(2,725,318) 이 배율 문제와 무관하다 — 영향받는 건 `building`·`building_floor`·`unit` 뿐. 확정은 [C] 일괄 파일을 실제로 받아 필터링해 봐야 난다 |
| **전국 확장 시 Supabase 용량** | ✅ **실측으로 갱신 (2026-08-10 라이브 SQL)** — 옛 서술("전국 ~64GB vs 한도 8GB")은 두 곳이 부정확했다. ① 행당 바이트를 스키마에서 추정한 값이 마스터 표에서 체계적으로 낮았다(building **2.84배**·parcel 1.76배·building_floor 1.80배 빗나감. transaction만 0.63배 과대). 실측 B/행 = parcel 806·building 811·building_floor 717·unit 460·unit_business 596·transaction 247. ② **"8GB 한도"는 하드 캡이 아니다** — Pro 플랜 **포함분**일 뿐이고 초과분은 GB당 $0.125 자동 확장. 진짜 천장은 **컴퓨트 등급의 최대 DB 크기**(Micro 10GB / Small 50GB / Medium 100GB). 갱신 전망: 전국 마스터 **12.0~21.5GB**, 상권정보 **분기당 1.51GB**. 현재 DB 262MB | **단계별 판정**: [A]시드+[B]토지특성 = 약 2.6GB → **현 마이크로(10GB)로 승급 불필요** / [C] 건축HUB 전국(+약 15GB) → **Small 승급 필요**(+$5/월) / 상권정보 41분기 전량 → Medium(+$50/월). 최대 절감 레버는 인덱스가 아니라 **상권정보 분기 아카이브(8분기 유지)** — 월 $84 → $31 |
| ⛔ **`building.bcr`(건폐율)에 소스 오류가 있다 — 7행이 적재 23,351행을 막았다 (2026-08-11)** | 건폐율은 정의상 0~100%인데 원본 표제부 242,631행 중 **7행이 1만%를 넘는다**(최댓값 **79,095%** 종로 당주동 39-2 · 74,045% 중구 을지로6가 · 59,315% 성북 정릉동). `bcr numeric(6,2)`(최대 9,999.99)에 걸려 `22003 numeric field overflow`가 나고, **그 행이 속한 배치에서 적재가 멈춘다**. 5개 구(종로·중구·성북·양천·대전서구)가 부분 적재로 끝났고 building 219,280 / 242,631 이었다 | 처방 = `numeric(10,2)`로 확대(`2026-08-11d`). **값은 안 버린다** — "몇 %부터 오류인가"를 추측으로 정하면 되돌릴 수 없다. ⚠️ **대신 평균·분포 집계 때 반드시 상한을 걸 것** — 79,095% 한 행이 통계를 통째로 흔든다(컬럼 주석에도 박음) |
| ⛔ **파이프(`\| tail`)가 실패를 감춘다 — 거짓 "성공 29/실패 0" (2026-08-11)** | 구별 적재 루프를 `python … \| tail -18` 로 짰다. 셸 파이프라인의 종료 코드는 기본적으로 **마지막 명령(`tail`)** 것이라, 적재기가 `exit 1`을 내도 항상 성공으로 보인다. 그래서 위 bcr 결함으로 5개 구가 죽었는데 **"성공 29 / 실패 0"** 이라는 거짓 보고가 나왔고, 구별 raw↔DB 행수 대조를 하고서야 드러났다 | 처방 = 루프 맨 위에 **`set -o pipefail`**(주석으로 이유 명시). ⭐ 교훈: **"성공했다"는 스크립트의 자기 보고를 믿지 말 것.** 데이터 양(행 수)을 원본과 대조하는 게 유일하게 확실한 검증이다 |
| ⚠️ **흔한 단어 검색이 느리다 — 지번 가지를 더한 뒤 2배 (2026-08-11)** | `search_buildings`가 건물명·도로명주소·**지번** 3가지를 UNION으로 찾는다. 선별력 있는 검색어는 전부 빠르다: `역삼동 823-4` **~0ms** · `823-4` ~0ms · `테헤란로 117` ~0ms · `노보텔` ~0ms(순수 서버 시간, 왕복 24ms 제외). 그런데 `빌딩`처럼 선별력 없는 한 단어는 **763ms → 약 1,550ms**로 느려졌다(가지 2개 → 3개). 강남만 있을 때는 351ms였다 | ⛔ **원인 미확정** — ~~`EXPLAIN`을 봐야 하는데 이 PC에서 DB에 SQL을 못 돌린다(MCP·CLI가 다른 계정, `.env`에 DB 비밀번호 없음)~~. 가설: 지번 가지의 gin_trgm이 '빌딩'에 0건인데도 스캔 비용을 문다. 처방 후보 = ⓐ 검색어 길이·선별력으로 가지 건너뛰기 ⓑ 지번 가지를 숫자/`-` 포함 검색어에만 적용. ✅ **막힘은 풀렸다 (기록 2026-09-01)** — `.env` 의 `SANGGA_DATABASE_URL` 과 `python scripts/dbx.py --explain "…"` 가 이미 있다(psql 경유). **다음 행동은 "접속 문자열 넣기"가 아니라 그냥 EXPLAIN 을 떠 보는 것**이다 |
| ⛔ **`platGbCd`를 PNU에 그대로 쓰면 전량이 조용히 유실된다 (2026-08-11 실제로 당함)** | 건축HUB `platGbCd`는 PNU 대지구분과 **코드 체계가 다르다**: PNU `1`(대지)↔`platGbCd` `0`, PNU `2`(산)↔`1`. 이 변환을 빠뜨리고 일괄 파일을 변환했더니 서울+대전 표제부 **727,585행 중 132행만** parcel과 맞았다. **에러는 하나도 안 났다** — 나머지는 "parcel 미보유"로 세어지고 조용히 버려졌다. 실제 적재였다면 "성공"으로 끝나고 건물이 132개만 들어갔을 것. 라이브 검산: 파일 첫 행 `11110\|18400\|0\|0220\|0001` → 잘못 조립한 `1111018400002200001`은 **없고**, 올바른 `1111018400102200001`은 종로구 창의문로 170(지번 220-1)로 실재 | ⚠️ **이 함정은 `docs/상세계획.md` §3.1에 2026-08-07부터 적혀 있었는데 놓쳤다.** 새 소스를 붙일 때 "알려진한계·상세계획 먼저 읽기"를 건너뛰면 이렇게 된다. 처방 = `convert_bldrgst_bulk.PLAT_GB_TO_PNU` + 회귀 테스트(매핑에 없는 코드는 추측하지 않고 `None`). ➕ **`platGbCd='2'`는 블록/특수지번**(번·지가 `0000`, 특수지명 '제2토지구획정리구내'·'하천부지' 등) — 유효 지번이 없어 PNU 불가, **정상 제외**다(전국 37,378행 0.46% · 서울+대전 588행 0.081%). 통계에서 '진짜 이상'과 갈라 센다 |
| ✅ **[C] 건축물대장은 API가 아니라 일괄 파일로 받는다 (2026-08-11 해소)** | 건축HUB 대용량 서비스에서 **전국 3종 4.4GB를 실제로 받았다. 로그인 불필요.** 표제부 646.64MB(원본 3.51GB·**8,051,204행**) · 층별개요 679.96MB · 전유공용면적 3.06GB. 형식 = zip 안 `.txt` 1개, **`\|` 구분·UTF-8**(웹 검색이 말한 "ANSI"는 틀렸다 — 실측이 이긴다). 속도 = 646MB를 **35초**. **누적분(전국 전체)**이라 최신 1개만 받으면 된다(3개월 보관은 문제 아님). PNU 조립 실증: `…\|11110\|18400\|0\|0220\|0001\|…` → `1111018400002200001` | 아래 "API 일 한도" 줄의 **211일·118일은 이제 대비책**이다(파일 서비스가 끊길 때). 수집기 = `scripts/collectors/fetch_bldrgst_bulk.py`. **적재기는 아직 없다** — `load_vworld_bulk.py` 패턴으로 신설 필요(0005 A안 남은 일 3가지) |
| **API 일 한도 = 엔드포인트별 1만** | 강남 1개 구 완주에 약 3일. ⛔ **전국 산술 (2026-08-10)**: 한도는 **총합이 아니라 엔드포인트당**이므로(2026-08-07 실측 확정, `collect_building_ledger.py:85-88` — 전유공용 단독 9,208콜에서 차단·나머지 2종은 그날 5,6xx콜로 여유) 3종이 병렬로 돈다. 병목 = 전유공용면적: 1,099,599 × **1.818** ÷ 9,500 = **211일**. 표제부 116일 · **층별개요 118일**(배수가 1.0이 아니라 1.0155). ⚠️ **"197일"은 2026-08-10 2차 정정됐다** — `EXPOS_CALLS_MULTIPLIER`가 1.7이었는데 **데이터 0건 PNU에도 나가는 빈 응답 1콜**을 안 세어 약 7% 과소였다. 근거는 추정이 아니라 **`api_quota_log` 원장부 실측**(강남 3일 합: 표제부 12,283 · 층별개요 12,464 · 전유공용 22,317콜 ÷ `collect_progress` done 12,274 PNU). raw 독립 재계수 22,328콜과 **11콜(0.05%)** 차이로 교차검증됨. ⚠️ 그 이전 "330일"은 예산을 3종이 나눠 쓴다고 오해한 별개의 과대치 | ⛔ **[C]의 지배 제약은 용량·요금이 아니라 시간이다.** ✅ **단축 레버 4개 전부 확인 완료(2026-08-10 2차)**: ④ **일괄 파일이 실재한다** — 건축HUB 대용량 제공 서비스(월 20일 전월분, 회원 로그인, 표제부·층별개요·전유공용면적 전부 포함. 전국 규모 검산 = 표제부 786만행·전유공용 **1억947만행**). 파일 필드에 시군구·법정동·대지구분·번·지가 있어 **PNU 조립 가능** ⇒ 브이월드 일괄 CSV와 같은 패턴으로 적재 가능. **단 용량·형식·다운로드 절차는 로그인 벽에 막혀 미확인**(👤 1회 확인 필요, `open.eais.go.kr`은 이 PC에서 연결 거부) ① **전유공용을 집합건물에만 호출** — 표제부 `regstrGbCdNm`로 판별 **정확도 100%**(강남 11,773 PNU 전수, 놓침 0) ⇒ 콜 47.6% 절감, 211일 → **118일**. ⚠️ **118일 밑은 조건부만으로 못 간다** — 병목이 층별개요로 옮겨간다. ⚠️ 옛 근거 "18.35%"는 1일차 건물 단위, 호출을 지배하는 건 **PNU 단위 13.99%** ② **한도 증설 = 자동승인**(개발 1만 → 운영 최대 10만, 심의구분 개발·운영 **둘 다 자동승인**) ⇒ **리드타임 없음**. "심사가 리드타임이라 지금 신청" 전제는 폐기 ③ 범위 축소(조건부 기준): 서울만 **17일** / 수도권 **45일** / 전국+한도10배 **12일**. 상세: `docs/decisions/0005` |
| **브이월드 토지특성 전국 완주는 API로 사실상 불가** | 전국 상권 고유 PNU **1,099,599개**(2026Q1 상권정보 17개 CSV 전수 카운트, 2026-08-09 — 독립 재계수 2방법 일치. 분기 스냅샷 기준이라 필지 수는 분기마다 증감하는 움직이는 값). 파일럿(강남구 전체=11680, 12,274) 제외 잔여 1,087,325 ÷ 자체 캡 5,000콜/일 ≈ **218일** | 전국 확장 시 **일괄 파일(15048121, 전국 CSV/SHP·반기)**로 전환(2~3일 작업 추정). 파일 실물 실측(08-09): 목록은 로그인 불필요, **개별 다운로드는 로그인 필수**(버튼 JS `loginYn` 검사 + 비로그인 호출 시 본문 0바이트 — 서버도 차단). **CSV는 시·도 단위 17파일, 최신분(기준 2026-05-19) 합계 ≈0.8GB** — 807파일·19.3GB는 SHP 포함 총계. 서울 CSV 스모크 통과(08-09 밤): **889,017행 중 유효 19자리 PNU 888,805개(중복 0)**, 나머지 **212건(0.024%)은 특수 블록 지번**(진관동 등 지번에 `중`·`단`·`주` 문자 포함 — 정의상 19자리가 아니므로 적재 시 제외). 도로접면·공시지가 빈값 0%. ⚠️ 한때 "889,017행 전부 고유 PNU 19자리 + 212건 예외"로 적었던 것은 **한 문장 안의 자기모순**이었다(적대검증 2026-08-10 지적, 정정). API는 파일럿·증분용 유지. 결정 0003 참조 |
| **`numOfRows` 상한이 API마다 다르다** | 건축HUB **100** / 실거래(RTMS) **1000** | 착각해 건축HUB를 1000으로 바꾸면 **데이터가 에러 없이 누락**된다 |
| **PostGIS 시스템 표를 우리 권한으로 못 막는다** | `spatial_ref_sys` 소유자가 `supabase_admin`. anon INSERT·DELETE 가능 | ~~현재 서비스 영향 0(`ST_Transform` 호출 0건). **지도·반경 검색 착수 전** 노출 스키마 분리~~ → ✅ **처방 완료 (2026-08-24, 기록 2026-09-01)** — REST 노출 스키마에서 `public` 을 뺐다(`pgrst.db_schemas = 'api, graphql_public'`). 권한 자체는 여전히 못 회수하지만 **인터넷에서 닿지 않는다**. 유지 여부는 `post_load.py --check` 가 매번 [주의]로 지켜본다. ⚠️ 괄호 안 "`ST_Transform` 호출 0건"은 이제 사실이 아니다(상권 좌표 변환에 쓴다) — 그래서 이 표가 오염되면 조용히 틀어지는 것이 맞고, 그 방어가 곧 노출 차단이다 |
| ~~**새 Postgres 함수는 기본적으로 anon에게 열린다**~~ | ~~Postgres 표준 동작~~ | 🔸 **처방 정정 (2026-08-10, 라이브 실측 + 공식문서로 확정)** — `revoke all on function X from public;`은 **무효였다**. PUBLIC은 실제 롤이 아니라 가상 그룹이고, Supabase는 새 함수에 anon·authenticated 앞으로 **직접(directly)** EXECUTE를 GRANT한다. PostgreSQL 공식 문서(sql-revoke.html): "revoking ... from PUBLIC does not necessarily mean that all roles have lost ... privilege: those who have it granted directly ... will still have it". 라이브 실측: `mask_person_name`·`building_display_nm` 둘 다 `from public` 회수문이 이미 적용된 상태에서 anon 공개키 RPC 직접 호출이 **HTTP 200**이었다. **올바른 처방 = `revoke all on function X from anon, authenticated;`(역할을 직접 지목)** — `public`에 함수를 만들 때마다 이 형태로 회수 → 명시적 `grant`를 사람이 기억해야 한다. `2026-08-10_revoke_helper_fns_from_anon.sql`로 회수문을 바로잡았고, 회귀 가드 = `tests/test_display_name_sql_sync.py::test_helper_functions_are_not_granted_to_anon`(문자열 존재가 아니라 회수 대상에 anon/authenticated가 실제로 있는지 파싱해서 확인). ⚠️ **여기서 끝이 아니었다 (2026-08-13 정정)** — 그 회수를 라이브에 적용하자 **층별 스택 화면이 401로 죽었다**(`permission denied for function building_display_nm`). 원인 = 뷰가 본문에서 그 함수를 부르는데, **뷰 안에서 부르는 함수의 실행 권한은 뷰 소유자가 아니라 접속한 롤(anon)로 검사**된다(원본 표 읽기만 소유자 권한이다). 급히 되돌린 탓에 **정본은 '회수한다'인데 라이브는 열려 있는 상태가 3일간** 지속됐다. ✅ **최종 해소(2026-08-13, `2026-08-13c_display_nm_stored.sql`)** = 표시명을 `building.display_nm` 저장 컬럼에 미리 계산해 두고 뷰는 컬럼만 읽게 바꿨다 → 함수를 닫아도 화면이 산다. 라이브 실측: 뷰 지문 동일(20,000행 md5 `dcf44e34…`), 242,631행 전부 함수 결과와 0건 차이, anon 기준 층 스택 **200** / 두 함수 **401**. 교훈 = **뷰가 함수를 부르면 그 함수는 사실상 공개해야 한다** — 값을 컬럼에 담는 것이 근본 처방이다. 그리고 그때 내 검증이 `select=pnu,floor_no`로 **컬럼을 좁혀 부르는 바람에 함수 호출을 우회**해 200을 보고 "안 깨졌다"고 오판했다 — 검증은 **화면이 실제로 보내는 요청 그대로** 해야 한다 |
| ~~**부동산원 R-ONE 실호출이 아직 0회**~~ | ~~키는 발급됐으나 `scripts/`에 관련 코드 0건~~ | ✅ **해소 (2026-08-08~09)** — 실호출 검증(#18)에 이어 수집기·적재기 신설(#27, `collect_rone.py`/`load_rone.py`, 테스트 109개). 라이브 파일럿 1건(집합상가 층별효용비율 2026Q2, 3,514행) 실측 완료. ✅ **전 단계 완료(2026-08-09 오후, 사장님 승인)** — 스팟체크 23/23 유효(오피스 `TT` 접두 ID도 진짜) → 본 수집 288조합·360콜·raw 136,321행 → **`rent_stat` 7,232행 적재 + 라이브 재조회 검증**(지역수×공표 8분기 산식 일치, 2024Q3~2026Q2). ⚠️ 실측 정정: 시리즈 시작(2024Q3) 이전 분기는 quiet-zero가 아니라 **`INFO-200` 에러**로 온다 — 장부 failed 92건 전부 이것(진짜 오류 0건, `--retry-failed` 무의미). 구조적 스킵 3곳(raw엔 보존): 오피스 6층+ 2구간(§1) · 오피스 '영등포역'(유일하게 floor_util 표에 없는 지역, 5지표×8분기 스킵) · 2024Q2 임대가격지수(그 분기 floor_util 부재로 지역 대조표 불가) |
| ~~**NSDI 토지특성은 가입조차 안 했다**~~ | ~~도로접면(§상가 가격 최대 변수)의 원천~~ | ✅ **전제 자체가 낡았었음 (2026-08-09 정밀조사)** — NSDI 포털(nsdi.go.kr)은 **2024-01-01 서비스 종료**(DNS 사망 실측), 기능은 **브이월드(vworld.kr)로 통합**. 토지특성(도로접면)은 ① 공공데이터포털 파일데이터 15048121(전국 CSV/SHP, 반기 갱신, 무료·제한없음, 다운로드는 브이월드 경유) ② 브이월드 데이터 API(`api.vworld.kr/ned/data/…`, PNU 조회, 공공데이터포털 등재본 15123549는 개발단계 자동승인) 두 경로. **브이월드 무료 회원가입·인증키 발급 완료(2026-08-09, 👤 — `.env`의 `VWORLD_KEY`, 36자 UUID)**. ✅ **토지특성 API 라이브 실측 완료 (2026-08-09)** — 스펙 전문은 `docs/상세계획.md` §1.2 브이월드 박스. ⚠️ **여기 남기는 함정 하나: `domain` 파라미터가 키에 등록된 서비스 URL과 일치해야 한다.** 안 맞으면 `resultCode=INCORRECT_KEY`("인증키 정보가 올바르지 않습니다")가 오는데 — **권한 오류처럼 보이지만 실제로는 도메인 불일치다.** `localhost` 계열 6종으로 시도해 전부 실패했고, 콘솔에서 등록값(`https://github.com/developer-duno/sangga`)을 확인해 넣자 즉시 통과했다. 호스트만 맞으면 되고(`github.com`도 통과), 등록 URL을 바꾸면 호출 코드도 같이 바꿔야 한다. 개발키 만료 **2027-02-09**(6개월, 최대 3회 연장). |

## 5. 설계·검증 방법론의 한계

| 한계 | 근거 |
|---|---|
| **`v_floor_stack`이 전역 최신 분기로 점포를 고른다** | `snapshot_ym = (select max(snapshot_ym) from unit_business)` — 지역 필터가 없다. 여러 지역·분기가 쌓이면 최신 분기에 없는 지역이 **통째로 빈 층**으로 보인다. Phase 5 착수 전 필수 수정 |
| **`v_unit_current`를 스택뷰에 쓰면 전 호실을 공실로 오판** | `unit_id`가 전 행 NULL이라 63,717행 100%가 공실 판정. 뷰 주석에 ⛔ 명시 |
| **`unit_business (pnu, floor_no)` 인덱스가 없다** | `v_floor_stack`의 점포 lateral이 정확히 이 조합으로 필터. 강남 6.4만 행에선 무해, 전국이면 필요 |
| ~~**`v_coverage_stats`는 화면을 열 때마다 전량을 센다**~~ → **해소(2026-08-22d 사전계산)** | 각주 숫자를 런타임 계산하려고 `count(*)`로 최신 분기 전체를 훑는다. 강남 6.4만 행에선 수십 ms라 무해하지만 **전국 수백만 행이면 화면 로드마다 부담**이 된다 — 그때는 집계 결과를 표에 적재하거나 materialized view로 바꾼다. 또한 `v_floor_stack`과 **같은 전역 최신 분기**를 쓰므로 지역별 분기로 바꿀 땐 **둘을 반드시 함께** 고쳐야 한다(따로 고치면 화면과 각주가 다른 분기를 말한다). ⚠️ **예측이 맞아떨어졌다(2026-08-13 라이브 재측정)** — 서울+대전 규모(63.5만 행)에서는 커버링 인덱스(당시 이름 `idx_ub_snapshot_floor`, `2026-08-11f`)로 버퍼 698·131ms까지 잡았었는데, 전국 시드(`unit_business` 202606분 2,772,484행) 이후 `explain (analyze, buffers)`로 다시 재니 **버퍼 36,150(shared hit 32,951+read 3,199)·실행시간 1,377ms**로 되돌아갔다. 원인은 인덱스 부재가 아니라 `Heap Fetches: 232,890`(인덱스온리 스캔인데도 힙을 되짚는다) — 대량 적재 직후라 visibility map이 아직 못 따라간 것이다. ✅ **재검토 종결 (2026-08-22) → 결국 사전계산으로 전환했다 (`2026-08-22d`)** — ① **`VACUUM` 은 힙 되짚기만 고친다**: `vacuum (analyze)` 한 번에 **Heap Fetches 0 · 화면 0.31초**(실측 근거는 `scripts/post_load.py` 상단 docstring). 그때는 이것으로 "집계표/materialized view 전환은 필요 없다"고 판단했는데 **그 판단이 같은 날 뒤집혔다** — ⚠️ 아래 ②의 지역 조건이 붙자 **힙에 안 가는데도**(Heap Fetches 0) 최신 스냅샷 277만 행마다 `substr(pnu,1,5)`를 잘라 열린 구 목록과 대조하는 비용이 남아 **순수 실행 2.1~4.9초**가 됐다(3회씩 반복 실측, 부하에 따라 흔들린다). anon 공개 호출의 statement timeout 3초를 **넘나드는** 값이고, 넘으면 화면은 각주를 null 로 조용히 강등한다(에러 없이 각주만 사라진다). 그래서 `mv_coverage_stats`(한 줄)로 **미리 계산**하고 뷰는 그 한 줄만 내보낸다 — **순수 실행 0.03~0.13ms**. 집계값은 적재 때만 바뀌므로 갱신은 `post_load.py`가 요약표 4개와 함께 돌리고, 그 `--check`가 "표에 굳은 분기 == 원본 최신 분기" 등식으로 낡음을 잡는다(⚠️ **2026-09-01 기록**: 그 뒤 `mv_district_industry_mix` 가 더해져 요약표는 지금 **5개**다 — 진실은 `scripts/post_load.py` 의 `REFRESH_MVS`. 위 "4개"는 2026-08-22 당시의 값이다). **교훈: `Heap Fetches 0`은 "빠르다"의 증명이 아니다** — 훑는 행 수 자체가 크면 여전히 느리다. ② **세는 범위 자체가 줄었다**: 각주는 전국이 아니라 **서비스 지역(화면에서 고를 수 있는 구)만** 센다(`2026-08-22a` — 2,772,484행 → **634,770행**). 전국을 세던 때는 결측률도 15.3%p 과장됐었다(50.3% vs 35.0%). ③ **커버링 인덱스는 `idx_ub_snapshot_floor_pnu`(snapshot_ym, floor_no, pnu)로 바뀌었다** — 새 where 절이 `pnu`를 읽으므로 `pnu`가 빠지면 Index Only Scan이 깨져 `2026-08-11f`가 고친 간헐 500이 그대로 재발한다. 옛 2컬럼 인덱스는 이것의 앞부분이라 같은 마이그레이션에서 지웠다 |
| **`count(*) over ()`는 lim을 줄여도 비용이 안 준다** | 정확한 전체 건수를 위해 매칭 전부를 센다(실측 lim=1 254ms vs lim=100 277ms). "N개 이상"이라는 부정직한 표기를 피하기 위한 **의도된 대가** |
| **선별력 없는 검색어는 인덱스가 오히려 손해다** | `로`처럼 거의 모든 주소에 들어가는 한 글자는 트라이그램 인덱스가 무용하고, GIN 조회+heap 재확인 + UNION 두 가지 훑기·중복제거 비용만 더해진다. 라이브 실측(2026-08-08): 정밀 검색 152→**51~70ms**(2~3배 개선) / 광범위 `로` 272→**351ms**(30% 악화). **실사용 경로가 정밀 검색이라 절충은 맞지만 "무조건 빨라졌다"고 기억하지 말 것** |
| **옥탑(99)과 실제 99층을 구분할 수 없다** | 규격 자체의 한계. 강남 실측 최대 \|층\|=35라 현재 영향 0 |
| **L4 기본 규칙("반경 500m 동일층 실거래 5건 이상")이 1·2층에서만 절반쯤 선다** | 강남 12,274필지 전수 계산(2026-08-08, 좌표 100%·실거래 1,637건 기준). 5건 이상 확보되는 필지 비율: **1층 56.4% / 2층 48.7% / 3층 25.3% / 5층 4.3% / 지하1층 0%**. 층을 안 따지면 92.1%지만 그건 상가에서 의미가 없다(1층과 5층은 값이 배 단위로 다르다). 바탕도 얇다 — 실거래를 가진 필지 **369/12,274 = 3.0%**, (필지·층) 조합 822개 중 2건 이상은 **39%** | §6.1·§7이 "전 층에 시세를 낸다"를 전제하면 성립하지 않는다. **3층 이상은 L5(지역평균) 폴백이 사실상 기본값**이 되므로, 자동 확장 규칙의 기대 분포를 이 숫자로 다시 잡아야 한다 |
| **백테스트 v1(2026-08-15, 서울+대전): 정확한 단계일수록 좁고, 1층이 가장 부정확하다** | 시간 분할(학습 ~2025-12 / 검증 2026-01~07, 집합 2,986건) 실측: L2(같은 필지·층) MdAPE 13.3%·커버 28.4% / L5(500m) 25.8%·23.7% / L6(법정동) 32.4%·92.8% / BASE(구 평균) 38.8%·100%. 사다리 걷기(운영)는 MdAPE 29.1%로 구 평균(38.3%)을 이기지만 **채택의 53.4%가 L6 폴백**이고, 층대별로는 **1층 45.2% > 2층 29.1% > 3층+ 21.9%** — 1층은 위치 미세차(코너·전면 등)로 단가 이질성이 커서 어떤 방법도 잘 안 맞는다. 사다리가 구 평균에 지는 구도 있다(종로·성동·성북·광진·금천·관악·송파·강동 등). 파이썬·SQL(PostGIS) 이중 구현 대조로 소수점까지 일치 검산. 상세 `docs/backtest/성적표-v1.md` — Stage B 재결재(결정 0012 §2) 재료, 그 전엔 추정값 화면 출력 금지 |
| **그럴듯한 일치율이 판별력 0일 수 있다** | 층 목록 대조 99.8%가 **난수 300회에도 100% 일치**했다. 근거를 세울 땐 **귀무가설(랜덤) 대조를 반드시 병행** |
| **`ORDER BY` 없는 `LIMIT`은 매번 다른 결과** | 검증관 스스로 이 함정을 밟아 213개를 227→218로 잘못 셌다. 페이지네이션엔 항상 `order` 명시 |
| **v_floor_stack은 anon이 전량 스크레이핑 가능** | PostgREST 1,000행/요청 × 94회면 93,696행 전부. 원본이 공개 데이터라 위법은 아니나 **가공물 전량 복제는 가능** |
| **프론트 번들 ~~408KB(gzip 117KB)~~ → 489KB(gzip 141KB)** | 대부분 `@supabase/supabase-js` 전체(auth·realtime·storage 포함). 앱은 `.from()`·`.rpc()`만 쓴다. ⚠️ 수치 갱신 2026-09-01 — `pnpm build` 출력 기준(`index-*.js` 489.44 kB / gzip 141.07 kB, vite 의 kB=1000). 화면 조각이 늘며 20% 커졌다 |

## 6. 법률 (~~코드로 해소 불가 — 배포 게이트~~ → ✅ 2026-08-22 결정 0015 로 게이트 해제)

| 항목 | 상태 |
|---|---|
| "적정가격" 표현의 허용 범위 | ✅ **사장님 결재 (2026-08-22, 결정 0015)** — "폭(밴드)+근거" 현행 방식 가능. 금지어(적정가격·감정가·평가액)는 계속 금지 |
| **상호명 노출** (특히 폐업 추정 표시) | ✅ **사장님 결재 (2026-08-22, 결정 0015)** — 표시 가능. 변호사 검토가 아니라 사업 판단임을 결정 문서에 명기 |
| ~~**건물명 칸에 개인 성명이 들어 있다**~~ | ✅ **화면 노출 차단 (2026-08-08e)** — `bld_nm` 5,361개 중 **93개**가 `역삼동 610-2 단독주택 (김남연)` 형태였다(괄호 안 값의 빈도가 거의 전부 1회 = 사람 이름의 특징). `mask_person_name()`이 내보낼 때 지운다 — 뷰·검색 함수 둘 다 가려진 값만 내보내므로 브라우저 개발자도구로도 원문을 못 본다. 검색도 가려진 값만 보므로 `김남연`으로는 **0건**이다. 괄호 안이 건물 지칭어면 안 지운다(`썬프라자(태양빌딩)`·`은하빌딩2(신관)` 보호 — 실측 4건). **원본 표 `building.bld_nm`은 그대로 둔다**(판단이 바뀌면 되돌릴 수 있게). 참고: 발견 시점에 앱이 미배포·공개키 미커밋이라 **실제 외부 노출은 0**이었다. ✅ **라이브 검증(2026-08-08)**: `김남연`·`안제호` 검색 **0건**, 화면 표시 `역삼동 609-26 제1종근린생활시설 (안제호)` → **`역삼동 609-26 제1종근린생활시설`**, 가려진 건물 수 **93개**(예측치와 일치) |
| 상권정보 상업적 재가공 범위 | ✅ **사장님 결재 (2026-08-22, 결정 0015)** — 집계·가공 표시 가능. 공공누리 출처표시 의무는 유지 |
| 상가건물임대차보호법 2026-05-12 개정분 반영 | ✅ **조사 완료 (2026-08-22, 결정 0015 §2)** — 개정 핵심은 임대인 관리비 공개 의무라 우리와 무관, 현 화면 문구 충돌 0. 임대 카드 신설 시 "관리비 불포함" 한 줄 지침 |

---

## 이 문서를 쓰는 법

- **조사 전에 먼저 읽는다.** 여기 있으면 다시 조사하지 않는다.
- **새 한계를 찾으면 추가한다.** 근거(파일:줄 또는 실측값)와 "언제 다시 봐야 하나"를 함께.
- **해소되면 지우지 말고 취소선 + 해소 근거**를 남긴다. 왜 이제 괜찮은지가 다음 사람에게 필요하다.
- 진행 중 작업·우선순위는 여기가 아니라 `PROGRESS.md` 소관이다.
