# 실행 로드맵 (2026-08-22 확정) — 발굴 26건 + 기존 로드맵, 적대검증 반영판

> **만든 방법**: 창의 발굴 3방향(사용자 여정·데이터 자산·시장 차별화, 제안 26건) → 플랜 v1 →
> **플랜 자체를 적대검증 4방향**(개별 주장 실측 반박 3 + 구조 비평 1)으로 교정한 v2다.
> 검증에서 반박된 것(구 칩 시세 배지 "공짜" 주장 · 건물 스펙 "100% 채움" · 기준시가 비율 수치 등)은
> 이미 반영돼 있으므로 **재조사하지 말 것**. 근거 상세는 메모리 `plan-v2-verification-2026-08-22`.
>
> **규칙**: 각 항목의 "선행조건" 칸이 비어 있지 않으면 그것부터. Wave 안 순서는 자유, Wave 간 순서는 고정.
> 실거래를 새로 적재한 Wave 는 성적표 재생성 여부를 **통과 구 변동 가능성 보고와 함께** 사장님께 묻는다(결정 0013 §4).
> 새 화면 섹션마다 **사장님 눈 검증 1건**(👤)을 그 PR 의 완료 조건에 포함한다.

## Wave 0 — 배포 트랙 (즉시 시작, 다른 Wave 와 병렬)

> ⭐ **법률 4건 사장님 결재 완료 (2026-08-22)** — 변호사 검토 관문을 **사장님의 사업 판단**으로
> 갈음한다(원 문서들은 "변호사 검토 권장"이었음 — 그 사실과 함께 기록):
> ①상호명 표시 **가능** ②상권정보 재가공 **가능** ③참고 시세 표현 **가능**(단일 값이 아니라
> **폭으로 알려주는 지금 방식 유지**가 조건 취지) ④상가임대차법 2026-05-12 개정분은 Claude 조사로
> 갈음 — 개정 핵심은 임대인의 관리비 14항목 공개 의무라 정보 서비스와 무관, 현 화면 문구 충돌 0
> (실측: 임대차 관련 문구는 "사고파는 값이고 월세·보증금이 아닙니다" 1건뿐).
> 향후 지침: 임대 카드에 "임대료 통계에 관리비 불포함" 한 줄 · 안내 페이지에서 임대차를 다루면 관리비 공개 의무 반영.
> ⇒ **배포는 이제 순수 기술 작업**이며, 상호명이 걸려 있던 항목(상호명 검색·인쇄물 상호명·폐업 추정 표시)의
> 법률 게이트도 해제된다(폐업 표시는 기술 선행 결함이 남아 있어 여전히 후순위).

| 항목 | 선행조건 | 내용 |
|---|---|---|
| PostgREST public→api 전환 | 없음 (조사는 2026-08-08 완료 — 재조사 금지) | ✅ **전제 2개 실행 확인 완료(2026-08-22, 도커 실측)** — 노출 목록에서 public 제거 가능(PGRST106/406 차단) · pass-through 뷰 업서트 가능(PK 자동 감지가 뷰 관통, 수집기 코드 변경 0). 마이그레이션 `2026-08-22e_api_schema.sql` + 화면 `db:{schema:'api'}`. ✅ **라이브 전환까지 완료(2026-08-22 실측)** — 대시보드 노출 = `api, public, graphql_public`. ✅ **꼬리까지 종결(2026-08-24, 배포 직후)** — `alter role authenticator set pgrst.db_schemas = 'api, graphql_public'` + `notify pgrst, 'reload config'`/`'reload schema'` 로 **옛 문(public) 닫음**. 검증 3종: ①`Accept-Profile: public` → **PGRST106 "Invalid schema: public"** ②새 문 RPC 200 ③라이브 화면 검색→층 스택→카드 5장 정상. 되돌리려면 같은 명령에 `'api, public, graphql_public'`. ⚠️ **노출의 진실은 authenticator 롤 설정**(대시보드 화면엔 안 보임)이라, 되돌림 감지를 `post_load.py --check` 에 넣었다(`public_rest_exposed`) — 누가 되돌리면 [정상] 이 [주의] 로 바뀐다 |
| 배포 설정 + 첫 배포 | 위 항목 | ✅ **완료 (2026-08-24)** — 라이브 **https://sangga-one.vercel.app** (`sangga.vercel.app` 은 선점돼 -one 배정). Vercel 프로젝트 `sangga` = GitHub `developer-duno/sangga` 연결이라 **main push 가 곧 배포**. env 는 문서의 "2개"가 아니라 **3개**(`VITE_SUPABASE_URL`·`VITE_SUPABASE_ANON_KEY`·**`VITE_KAKAO_JS_KEY`** — 빠지면 지도만 빈 화면) × 운영·미리보기. 라이브 실측: 검색→층 스택→카드 5장(펼침 4)·콘솔 0. ⚠️ CLI(v50.41.0)의 미리보기 env 등록은 버그라 REST API 로 넣었다. **꼬리 둘도 같은 날 종결**: ①카카오 콘솔 등록 ✅ — 미분양아파트 앱(1398824) JS 키의 **JavaScript SDK 도메인**에 `https://sangga-one.vercel.app` 추가(재배포 없이 즉시 지도 라이브 확인). ⛔ 새 콘솔은 도메인을 "플랫폼>Web"이 아니라 **"플랫폼 키 > JS 키 수정"**에서 관리한다 · ⛔ **카카오맵 무료 쿼터는 옛 앱들만** 갖고 있어(계정 10개 중 6개) 새로 만든 "상가분석" 앱(1545321)은 켜면 과금 — 그래서 미분양 키 공유 유지(사장님 결정). ②노출에서 public 제거 ✅ — 위 행 참조 |

## Wave 1 — 진행 중 (2026-08-22 당일)

| 항목 | 상태 |
|---|---|
| 업종 분포 + 업종 골라보기 (상권 지표 1단계) | ✅ 완료 — PR #77 머지·라이브 (2026-08-22, 사전계산 62,457행·최악 중구 44.5ms) |
| 각주 사전계산 픽스 | ✅ 완료 — PR #75 + 라이브 적용 (0.03~0.13ms 실측), 당일 리뷰 반영분까지 머지 |
| 전남광주 실거래 3,921행 적재 | ✅ 완료 (화면 노출 없음 확정 — 데이터 준비 트랙) |

## Wave 2 — 공짜 묶음 (검증 통과분만)

| 항목 | 선행조건 | 핵심 규칙 (검증이 확정) |
|---|---|---|
| PR-A: 도로접면 배지 + 업종 요약 헤더 + 구 칩 건물 수 | ✅ 완료 — PR #80 머지 (2026-08-22, vitest 269·E2E 10) | 도로접면은 **원문 그대로**(큰길/골목 재해석 금지 — 시세엔진 road_grade 와 이원화 방지), '맹지·지정되지않음' 671행 배지 생략. 업종 요약은 (PNU,층) 집계라 **동 중복 경고보다 아래** 금지 — 경고를 위로. 문구에 '적정가·평가액·감정가·가치평가·시세' 금지(E2E 가 부재 단언) |
| PR-B: 건물 스펙 4칸 (연면적·용적률·건폐율·주차) | ✅ 완료 — PR #84 머지 (2026-08-24, 뷰 마이그레이션 2026-08-24a 라이브 적용·실측까지 완료) | ⛔ **0 = 미상**(대장이 미기재를 '0'으로 줌 — 롯데월드타워가 주차 0·건폐율 0 실측. 값으로는 "정말 0"과 "안 적힘"을 못 가름) → 0 은 "미상"으로 표시. ⓘ "NULL 0행"은 **현재 적재분(서울·대전 30개 구) 관찰값**이지 구조적 보장이 아니다 — 적재기는 결측을 NULL 로 쓴다(`load_building_ledger.py` 의 `_to_float`·`sum_parking`). [C] 전국 적재 후 다시 셀 것(화면은 0·null 을 같은 "미상"으로 처리하므로 어느 쪽이어도 안 깨짐). bcr≤100 상한 + 연면적 이상치(최대 861만㎡) 상한. `create or replace view` 는 칼럼 끝 추가만, drop 금지. **라이브 뷰 실측이 머지 조건**(E2E 는 REST 모킹이라 라이브 드리프트를 못 잡음) |
| 한 장 요약 접힘 틀 (0014 공통 뼈대 승격) | ✅ 이 PR 로 완료 (개선 Batch 4 — 카드 5장·펼침 4장, vitest 313·E2E 12) | 섹션 표시 예산: 첫 화면 펼침 상한 4개, 나머지 접힘. 새 카드에 **역할 태그**(투자자/창업자/중개사)와 기본 접힘 여부를 처음부터 부여. **구현 결과** — 배치표 정본은 `src/lib/sectionCards.ts` 의 `SECTION_PLAN` 한 곳(제목·역할·기본 펼침), 상한 상수는 `SECTION_EXPAND_BUDGET = 4`. 카드 다섯 장 = 속한 상권(공통)·층 목록(공통)·둘레의 업종 분포(창업자)·실거래 기록(투자자) **펼침** + 참고 매매 시세(투자자) **접힘**. 접는 기준은 자리가 아니라 역할이다 — 공통은 전부 펼치고 역할마다 하나씩만 펼쳐 **사실이 먼저, 추정은 펼쳐 봐야 보이게** 한다. ⛔ **접힘은 숨김이 아니다** — 접힌 카드도 제목 + 핵심 한 줄(`.card__summary`)은 그대로 보인다(상권 카드는 **출처까지** 요약에 둔다 — 공공누리 1유형 의무). ⛔ 요약 줄에 추정 **값**을 적지 않는다(근거를 함께 실을 자리가 없다 — 절대 규칙 3). 상한이 조용히 깨지는 것은 `src/lib/sectionCards.test.ts` 의 가드가 잡는다. **독립 리뷰 반영** — ① 층 목록을 접어 둔 채 시세 줄을 눌러도 그 층이 보이게(`openSignal` 카운터 + 다음 그림으로 미루는 스크롤), 접힌 동안에는 시세 줄의 `aria-expanded` 가 거짓말을 안 하도록 `openFloor` 를 가린다 ② **본문 없는 카드(불러오는 중)는 버튼을 안 만든다**(눌러도 아무 일 없는 버튼은 읽어 주는 기기에 거짓말) · 본문이 있으면 `useId()` 로 `aria-controls` ↔ 패널 id 한 쌍 ③ 층 상세 제목 h3→h4(형제 `tx__sub`·`mix__sub` 와 계층 정합) |
| 제외: 구 칩 시세 제공 배지 | — | ❌ 검증 반박 — 서버 변경 + "구 절반에 부정 배지" 제품 결재 필요. 별건으로 |

## Wave 3 — 창업자 여정 + 사실 카드

| 항목 | 선행조건 | 핵심 규칙 |
|---|---|---|
| 지도 상권 이름 라벨 | geojson 재굽기(PROPERTY_KEYS 에 center 좌표 추가 — geojson 에 좌표가 없음이 실측 확인됨) + post_load 신선도 게이트 동기 | 줌 게이트 필수(강남 103면 동시 라벨) · DB center 8곳은 도형 밖 — 재굽기 시 ST_PointOnSurface 로 · DistrictMap.test.tsx 의 SDK 통째 모킹에 CustomOverlayMap 추가 선행 |
| 상권 → 건물 다리 (역방향 RPC) | 정렬 기준 결정(👤 1회: 층수순? 점포수순?) | 상한 설계 근거 = **명동 1,098동**(면적 최대 ≠ 건물수 최대 — 실측). security definer + 회수 패턴. 경계 걸침 필지는 st_contains 라 빠짐을 문구로 |
| 임대 **사실** 카드 (R-ONE 원본) | 없음 — 단 **역산 금지** | ⛔ 건물별 역산(추정×계수)은 임대 백테스트 + 결정 0015 결재 후 별도(게이트 우회 금지 — 구조 비평 CRITICAL). 사실 카드 규칙: 상권/구 단위 원본만 · **건물 기준 도달률 29.6%**(서울 10곳 중 7곳 "부동산원 조사 대상 아님" 정직 표기) · bld_type 4종 명시 선택(안 고르면 4배 부풀음) · floor_util "6+" 키는 Number() 파싱 불가 · 연 4배 환산 금지 · 겹침 상권 7동은 결정 0011 방식 |
| 공유 링크 + 새로고침 복원 | ✅ **완료 — 2026-08-25 (결정 0019)** | ⛔ **선행조건이 틀려 있었다**: "`p.lat`/`p.lng` 2줄"이 아니라 **`st_y(p.geom)`/`st_x(p.geom)`** 이다. parcel 의 좌표 칸과 geom 은 다른 시점에 채워지는 별개 칸이라, 칸을 쓰면 **들어온 길에 따라 마커 자리가 갈리고** "마커는 상권 밖인데 글자는 상권 안"이 된다(2026-08-14e 규칙). 새 RPC 불필요는 맞았다(마이그레이션 `2026-08-25a` — public 뷰 + **api 쌍둥이** + 스키마 리로드 3종). ⛔ **rewrite 도 불필요**했다 — 그 경고는 폴더 방식 전제였고 **물음표 방식**(`/?sgg=&bld=`)을 골라 `vercel.json` 을 안 건드린다(rewrite 는 상권 지도 파일까지 첫 화면으로 바꿔칠 위험을 진다). ⛔ **되살리는 중 주소 쓰기 금지**(적으면 링크가 열리자마자 사라진다 — 돌연변이 시험으로 가드 유효성 확인). 층 0행 239동은 "찾지 못했습니다" 안내. 로그인 없이 동작. 라이브 검산: 표본 1,999건 좌표 어긋남 0 · 검색↔층목록 5건 일치 · 노출 13개 그대로 |
| Wave 3 SQL 은 한 마이그레이션으로 묶음 | — | 번호는 22e 부터(22c·22d 선점됨). 라이브 적용은 Claude(dbx) — 사장님 위임 2026-08-22 |

## Wave 4 — 신뢰 자산

| 항목 | 핵심 규칙 |
|---|---|
| 성적표 공개 + 방법 공개 (한 쌍) | ⛔ 원본 성적표 그대로 금지 — **화면 체감 단계 분포 중심**(L6 폴백 53.4% 맨 앞, 커버리지 93.5%가 유리한 숫자라는 정직 공지 포함 — 결정 0013 §3). CSV 는 **5종**(1층유형별지표 포함). 숫자 복사 금지 — 서버/CSV 에서 읽고 버전 스탬프. 금천구 줄에 탈락 사유. 게이트 읽기 함수 신설(표는 anon·authenticated 둘 다 닫혀 있음 실측) |
| 자료 신선도 스탬프 (/자료) | "다음 갱신 예정일" 칸(분기 2026-10-31). 하드코딩 0 — 전 수치 런타임. api_quota_log 숫자 인용 금지(하한선일 뿐) |
| 넘기기 링크 구역 + 안내 페이지 | 0014 §5 채택안. 링크 목록은 자료 한 곳. 경쟁사 서술 = 사실 + 날짜 스탬프만 |
| 문의 버튼 3종 → **익명 의견함 1개로 확정** | ✅ 완료 — 2026-08-24 (결정 0016). ⛔ **"버튼 3종"이 아니라 하나다.** 셋으로 나누려면 각각 다른 곳으로 보내야 하는데 이 앱엔 서버가 없다(`api/` 폴더 0개). 화면 아래 한 자리에서 받고, **무엇을 보던 중이었나(건물·구)는 자동으로 함께 실린다** — 그게 이 의견함의 값어치 전부다(사람이 손으로 적게 하면 대부분 안 적는다). ⛔ **이름·연락처 칸을 안 만들었다** — 받는 순간 개인정보처리방침·보관기간·파기절차가 따라붙는데, 문의가 아직 0건인 단계에서 그 의무부터 지는 것은 순서가 거꾸로다. 대신 **답장을 못 한다는 사실을 화면에 미리 적는다.** 표는 anon 에게 통째로 닫혀 있고 넣기 전용 함수 하나만 열려 있다(읽는 함수 없음). ★ **후속 완료 — 2026-08-24c(결정 0017)**: 만들어 놓고 **읽을 계기가 없던** 것을 닫았다. `feedback-digest.yml`이 매주 **숫자만** 받아(본문은 어떤 칸으로도 안 나간다) 새 글이 있거나 보관 기한(90일)을 넘긴 글이 남아 있으면 이슈를 연다 — 조용한 주엔 아무것도 안 한다. ⛔ **지우기는 밖에 안 연다**(api 통과 함수를 아예 안 만들었다 — 지금은 무해해도 언젠가 인자가 붙는 날 파괴 창구가 된다). 대신 편지가 들어올 때 창고가 스스로 치운다. ⛔ 상한은 **총량이 아니라 하루**(종류별 1,000) — 총량은 한 번 차면 90일간 정상 사용자를 영구히 막는다. ⚠️ 1,000 은 안전망일 뿐이라 주간 숫자가 쌓이면 조인다 |
| 하얀 화면 안전망 + 라이브 생존 감시 | ✅ 완료 — 2026-08-24 (결정 0016, 원래 이 표에 없던 항목). 배포 뒤에야 드러난 구멍이다: 렌더 중 오류가 나면 화면이 **통째로 하얘지는데** 이 앱엔 서버가 없어 **그런 일이 있었다는 사실조차 몰랐다.** `ErrorBoundary` 3곳(입구·지도·층별 화면) + 오류 자동 기록(**한 방문에 한 번만** — 렌더 오류는 되풀이되므로 빗장이 없으면 한 번의 사고가 창고를 채운다) + 6시간마다 밖에서 두드리는 감시. ⛔ **감시가 자바스크립트 묶음까지 보는 것이 핵심** — 반쪽 배포는 첫 화면이 200 이라 그것만 봐서는 절대 못 잡는다 |

## Wave 5 — 대형 (설계서 → 결재 → 구현)

| 항목 | 선행조건 |
|---|---|
| 국세청 기준시가 | ⛔ **출처 미상 확정(2026-08-22 사장님 답변 "모름")** — 설계서에서 국세청 공식 경로 재확보부터. 설계 필수 항목: 29/46↔12 매핑표(없으면 광주·전남 0% 조인) · 5시트 전량 파싱(첫 장만 읽는 함정) · PNU 고아 14% 처리 · 특수지 한글 텍스트 매핑 · 용량(현재 4.32GB — **2026 단년 또는 시도 필터가 안전선**, 3년 전량은 한도 근접) · "세금 기준, 시세 아님" 문구(절대 규칙 2) |
| 동네 20년 단가 흐름 | 지역(구·동) 카드로만 — 건물 카드 안 금지(pre-2024 는 PNU 0). 연도별 층 결측률 변동 → 각 점에 n 병기 |
| 호실 구성표 | (bld_id,ho) DISTINCT = **2,920,278**(옛 63,717 은 강남 파일럿 수치 — 인용 금지) · unit 은 anon 차단이라 함수 경로 |
| 임대 역산 | 임대 백테스트 + 결정 0015 결재 |
| 검색 더 보기 / 형제 동 목록 / 상호명 검색 | 상호명 게이트 해제됨(2026-08-22 결재) — 상호명 검색은 색인 186MB 기설치라 착수 가능 |
| 기술 부채 | ✅ 전량 완료(2026-08-23~24) — RADIUS_M 가드·exposure 쓰기권한 확장은 개선 Batch 1(#82), 상수 모듈 분리·E2E 모바일 프로젝트는 Batch 2. 남은 신규 부채는 발견 시 이 칸에 다시 적는다 |

## 이번에 안 하는 것 (의도적 — 이유 명시)

| 항목 | 이유 |
|---|---|
| [B] 토지특성 재실행(전국) | 서비스 지역은 이미 99.6% — 전국 채움은 **새 구를 열기 직전**에. 새 구 오픈 체크리스트에 "도로접면 채움 확인" 추가 |
| [C] 건축HUB 전국 적재 | Supabase 승급(👤) 선행. zip 4.4GB 로컬 보유 |
| 개업·폐업 diff / 생존기간 | 법률 게이트는 해제(2026-08-22 결재), 그러나 **기술 선행 결함**(점포 조회가 전역 최신 분기 — 지역별 분기 어긋나면 빈 층) + biz_no 불연속 + 결측 변동(층 결측이 분기마다 달라 증감이 왜곡)이 남음 — "숫자만" 판이라도 결함 해소 후 |
| 성적표 v2 재생성 | 전남광주 적재로 입력이 변했으나 **통과 구가 조용히 바뀔 위험** — 변동 가능성 보고 후 사장님 결정으로만 |
