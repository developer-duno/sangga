# 상가 플랫폼 — 시작 명령어

> 위에서 아래로 순서대로. 각 단계 끝에 **확인 방법**이 있으니 넘어가기 전에 눈으로 보세요.

---

## STEP 1. 폴더 만들기 (터미널)

```bash
cd F:/                       # 원하는 위치로. mibunyang이 F:에 있으니 옆에
mkdir sangga && cd sangga
git init
mkdir -p docs supabase scripts/collectors src data/raw src/scoring
```

**확인**: `dir` 또는 `ls` 쳐서 폴더 6개가 보이면 OK

---

## STEP 2. 파일 배치

다운로드한 파일들을 이렇게 넣으세요.

```
sangga/
├── CLAUDE.md                       ← 받은 파일 그대로
├── supabase/schema.sql             ← 받은 파일 그대로
└── docs/
    ├── 상세계획.md                  ← 상가플랫폼_상세계획.md 이름 바꿔서
    ├── 데이터전수조사.md
    ├── 검색리포트설계.md
    └── 수집실행_시각화설계.md
```

**확인**: `type CLAUDE.md` (윈도우) 또는 `cat CLAUDE.md`로 내용이 보이면 OK

---

## STEP 3. Supabase 새 프로젝트 (브라우저)

1. https://supabase.com/dashboard → **New project**
2. 이름: `sangga` / 리전: **Northeast Asia (Seoul)** / DB 비밀번호는 따로 저장
3. 생성 대기 2~3분
4. **SQL Editor** → **New query** → `supabase/schema.sql` 전체 붙여넣기 → **Run**

**확인**: 왼쪽 **Table Editor**에 테이블 9개가 보이면 성공
`parcel, building, unit, unit_business, transaction, district, rent_stat, collect_progress, api_quota_log`

> PostGIS 확장 오류가 나면 Database → Extensions에서 `postgis`, `pg_trgm`을 먼저 켜고 다시 실행

5. **Settings → API**에서 아래 3개 복사

---

## STEP 4. 환경변수

`sangga/.env` 파일을 만들고:

```env
# Supabase (상가 전용 — 기존 mibunyang과 다른 값이어야 함)
SANGGA_SUPABASE_URL=https://xxxxx.supabase.co
SANGGA_SUPABASE_ANON_KEY=
SANGGA_SUPABASE_SERVICE_KEY=

# 공공데이터포털 (기존 키 그대로 사용 가능)
MOLIT_KEY=
SBIZ_KEY=
BLDRGST_KEY=
REB_KEY=

# 카카오맵
KAKAO_JS_KEY=
KAKAO_REST_KEY=
```

`.gitignore`에 추가:
```bash
echo ".env" >> .gitignore
echo "data/raw/" >> .gitignore
echo "node_modules/" >> .gitignore
```

**확인**: `git status`에 `.env`가 안 보이면 OK

---

## STEP 5. 공공데이터포털 활용신청 (브라우저)

**기존 계정 그대로. 새 계정 필요 없습니다.** 트래픽 한도는 API별로 따로 붙습니다.

| 신청할 것 | 주소 |
|---|---|
| 상업업무용 매매 실거래가 | data.go.kr/data/15126463/openapi.do |
| 소상공인 상가(상권)정보 | data.go.kr/data/15012005/openapi.do |
| 건축물대장 정보서비스 | hub.go.kr 또는 data.go.kr 검색 |
| 부동산원 부동산통계 | data.go.kr/data/15134761/openapi.do |

각 페이지 우측 상단 **활용신청** → 목적 입력 → 신청.
대부분 자동승인(1~2시간), 일부는 24시간 이상.

**확인**: 마이페이지 → 오픈API → 개발계정에 4건이 "승인"이면 OK

---

## STEP 6. ★ 상권정보 파일 다운로드 (오늘 꼭)

**이게 가장 중요합니다. 소급 수집이 안 됩니다.**

1. https://www.data.go.kr/data/15083033/fileData.do
2. 최신 분기 파일 다운로드
3. `sangga/data/raw/sangkwon/2026Q3.csv`로 저장 (분기명은 실제 파일 기준)

**확인 + 저에게 알려주실 것**:
```bash
# 행 수
wc -l data/raw/sangkwon/2026Q3.csv

# 용량
ls -lh data/raw/sangkwon/

# 컬럼 확인 (첫 줄)
head -1 data/raw/sangkwon/2026Q3.csv
```

윈도우 PowerShell이면:
```powershell
(Get-Content data\raw\sangkwon\2026Q3.csv | Measure-Object -Line).Lines
Get-ChildItem data\raw\sangkwon\ | Select Name, @{n='MB';e={[math]::Round($_.Length/1MB,1)}}
```

이 숫자를 알려주시면 볼륨 가설을 실측값으로 갱신하겠습니다.

---

## STEP 7. Claude Code 시작

```bash
cd F:/sangga
claude
```

첫 프롬프트로 이걸 넣으세요:

```
CLAUDE.md와 docs/상세계획.md를 읽어줘.
Phase 0 진행 중이고, 상권정보 분기 파일을 data/raw/sangkwon/에 받아뒀어.

첫 작업: 상권정보 CSV를 읽어서
1) 마포구(법정동코드 11440)만 필터
2) PNU·층정보·호정보 정규화 (층은 지상n=n, 지하n=-n, 옥탑=99, 불명=NULL)
3) Supabase의 parcel / building / unit / unit_business 테이블에 적재
4) 각 단계 행 수와 조인 성공률을 print로 출력

복사-붙여넣기로 바로 도는 완전한 스크립트로 만들어줘.
```

---

## STEP 8. 스킬 등록 (선택, 권장)

`budongsan-data.skill` 파일을 Claude Code에 등록하면 PNU 조립·층 정규화·API 카탈로그를 매번 설명 안 해도 됩니다.

이미 claude.ai 프로젝트에 등록하셨다면 Claude Code에서도 쓰려면 별도 등록이 필요합니다.

---

## 순서 요약

| # | 할 일 | 소요 | 놓치면 |
|---|---|---|---|
| 1 | 폴더 생성 | 1분 | — |
| 2 | 파일 배치 | 2분 | — |
| 3 | Supabase + 스키마 | 10분 | — |
| 4 | .env | 5분 | — |
| 5 | API 활용신청 | 10분 (승인 대기 별도) | 수집 못 함 |
| 6 | **상권정보 파일** | 30분 | **영구 손실** ★ |
| 7 | Claude Code 시작 | — | — |

**6번만은 오늘 하세요.** 나머지는 내일 해도 됩니다.

---

## 막히면

| 증상 | 해결 |
|---|---|
| PostGIS 오류 | Database → Extensions에서 `postgis` 켜고 재실행 |
| 스키마 일부만 생성 | Run은 전체를 한 번에. 나눠 실행하면 참조 순서가 깨짐 |
| API 승인이 안 남 | 심의승인 대상은 2~3일. 상권정보 파일은 승인 없이 받을 수 있음 |
| CSV 한글 깨짐 | UTF-8. 엑셀로 열지 말고 파이썬 `encoding='utf-8'`로 |
| 다운로드가 너무 큼 | 전국 파일이 맞음. 시도별 분할 파일도 있으니 서울만 받아도 됨 |
